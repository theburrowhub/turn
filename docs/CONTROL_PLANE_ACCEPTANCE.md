# Operator control-plane acceptance

**Status:** normative proof plan for the accepted post-v0.1 target; it does not claim these scenarios pass.

This matrix makes product completion reproducible. Every frozen requirement has exactly one primary proof
obligation. A primary obligation may name several evidence classes because a deterministic fake proves
semantics while a live or packaged run proves that a real integration supplies the promised evidence.

Evidence classes:

- **A — automated:** deterministic unit, property, integration, protocol or native snapshot test in CI;
- **D — destructive/recovery:** crash, race, disconnect, stale-generation or resource-pressure harness;
- **L — live:** opt-in authenticated run against the exact external CLI/provider version recorded in the
  evidence manifest;
- **P — packaged/platform:** signed/ad-hoc packaged application on each claimed OS, including assistive or
  device behavior that a headless fake cannot prove;
- **M — measured:** declared workload/hardware, raw measurements and enforceable regression budgets.

An acceptance record stores commit, build/version, platform, adapter/provider versions, fixture or account
scope, timestamps, command, result and artifact hashes. Unsupported capabilities need degradation evidence,
not a skipped test. “The PR merged”, “CI is green”, a screenshot, a mocked provider or a count of tests is
never sufficient by itself.

## Outcome, hierarchy and views

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-OUT-001` | `PRD-OUT-001` | A, M, P | At the design workload, an operator creates work, reaches every pending action through Attention and identifies every running/finished unit without opening panes to poll. |
| `ACP-OUT-002` | `PRD-OUT-002` | A, L | One mixed Session runs all supported dedicated adapters plus shell, TUI and log nodes; shared behavior is uniform and capability gaps are explicit. |
| `ACP-OUT-003` | `PRD-OUT-003` | A, D | Every interrupting surface consumes the same ordered queue/revision; no Layout, provider widget or telemetry path can move focus independently. |
| `ACP-OUT-004` | `PRD-OUT-004` | A, P | Recorded pointer/keyboard journeys meet §3.3 exactly: attach and Attention use at most one action, default create/Flow launch two, and unresolved authority is one consolidated review; no generic start or redundant confirmation appears. |
| `ACP-HIE-001` | `PRD-HIE-001` | A, P | Every durable unit appears under exactly one Workspace/Session root and all navigation/accessibility paths use that one tree. |
| `ACP-HIE-002` | `PRD-HIE-002` | A | A fixture gives the same nodes ancestry, group, dependency, context, lineage and message edges; each graph retains independent meaning and serialization. |
| `ACP-HIE-003` | `PRD-HIE-003` | A, D | Identity/cardinality property tests vary every id independently, enforce the §2.3 table, reject mismatched joins and migrate legacy aliases to distinct ids plus resolution-only tombstones. |
| `ACP-HIE-004` | `PRD-HIE-004` | A, D, L | Nested children receive stable identities/lifecycles, survive UI reconnect, reconcile after daemon/runtime recovery and never attach completion to a reused attempt. |
| `ACP-HIE-005` | `PRD-HIE-005` | A | Group add/move/delete and multi-Team membership alter only their named relationship; ancestry, context, checkout and execution authority remain unchanged and Team membership never moves a row. |
| `ACP-HIE-006` | `PRD-HIE-006` | A | Conflicting explicit/inferred/out-of-order edges keep the strongest evidence; unresolved children render unassigned with provenance. |
| `ACP-HIE-007` | `PRD-HIE-007` | A | A generated schema inventory matches domain/store/protocol/view/detailed-contract kind and relationship sets byte-for-byte; every kind round-trips and no vNext id aliases another type. |
| `ACP-HIE-008` | `PRD-HIE-008` | A, P | A node with spawn/process ancestry, one Group, multiple Teams and Flow membership renders once under fixed precedence; equal strongest evidence for two different parents remains ambiguous/unassigned, stable edge id orders only references, and Group moves leave semantic counts/Attention unchanged. |
| `ACP-HIE-009` | `PRD-HIE-009` | A, D, P | A 128-level persisted Group tree hydrates parent-first and round-trips exact order. Dedicated `create_group` plus Group-scoped non-Group resource/Browser creation carry operation id, preassigned Node id, exact Session/parent/GroupTree revisions and one CAS; lost reply/retry cannot duplicate. Self/indirect/concurrent cycles, cross-Session/non-Group parents and depth 129 fail atomically, subtree reparent/ungroup/promote/delete preserve descendants, runtimes and Attention, and fuzzed corrupt input terminates bounded with an isolated diagnostic. The binding machine proves proposed→current/refused/unbound, current→stale/unbound, stale→current/unbound and terminal refused/unbound. The scope machine proves provisioning→active/reconcile_required; active→missing/conflicted/unbinding/removing; missing/conflicted→active/unbinding; unbinding→unbound/reconcile_required; and removing→removed/reconcile_required. Each reconcile_required fixture asserts exact origin, last_proved, desired_terminal, possible_effect, original operation and receipt: provisioning reaches only active/unbound, unbinding only unbound and removing only removed, otherwise retaining the same record; no terminal desire weakens or changes kind. Only complete fresh active→missing/conflicted inventory atomically makes current→stale; failed/partial/stale evidence changes neither; proved recovery atomically makes missing→active and stale→current. Tagged present/absent binding proof is mandatory on bind/unbind/remove/reconcile and Group deletion; every illegal/raced scope, binding or GroupTree CAS refuses. `unbind_group_checkout_scope` and Group deletion terminalise only the projection binding and preserve scope/worktree; scope unbind/remove directly terminalise any proposed/current/stale binding without an observable stale intermediate, with the former retaining the worktree and only the latter allowed to reach removed after destructive proof. Adopted/dirty/unpublished/path-danger/remote generation cases never change cwd on presentation move, never delete by default and leave primary `main` switchable. |
| `ACP-HIE-010` | `PRD-HIE-010` | A, D, M, P | Dense nested, collapsed, filtered and variable-height fixtures derive identical `ProjectedRows` logical/accessibility preorder and a viewport-bounded `MaterializedRows` subset through restore, resize, zoom and concurrent topology. Every adjacent ProjectedRows pair satisfies `next.top = previous.bottom + TreeRowGap`, the token remains within 0–8 logical pixels at 100% zoom, and each virtualized spacer equals the exact omitted projected-height-plus-gap prefix sum. The oracle requires zero tidy/arrange prompt or action. A selected/focused row that remains projected is pinned materialized with stable focus/anchor. Collapse of its ancestor retains selection identity and focuses that ancestor; filtering retains identity and focuses the filter control then restores row focus when cleared; deletion chooses following projected sibling, then previous, then owning Session. Only the injected expansion/filter/topology reducer event may cause that fallback. Restore/resize/zoom emit no hierarchy or focus mutation; topology applies exactly its injected revision once and layout leaves canonical bytes/revision untouched. Runtime, input-owner and Attention remain byte-identical; an extreme or failed row is bounded without hiding/reordering siblings. |
| `ACP-VIE-001` | `PRD-VIE-001` | A, P | Selecting every hierarchy kind produces exactly one revisioned ViewTarget and one WorkSurface destination. |
| `ACP-VIE-002` | `PRD-VIE-002` | A | Selecting every child/resource changes no persisted Layout, process count, PTY input, Flow operation, unread flag or Attention state; Session activation is exercised only by ACP-LIF-009. |
| `ACP-VIE-003` | `PRD-VIE-003` | A, P | Snapshot/state-table tests cover content plus loading/empty/unsupported/disconnected/stopped/lost/stale for every NodeKind without generic fallback lies. |
| `ACP-VIE-004` | `PRD-VIE-004` | A, L, P | Dedicated Agent/subagent fixtures and live runs show every required region, exact parent/attempt and truthful missing fields. |
| `ACP-VIE-005` | `PRD-VIE-005` | A, P | Selecting live work attaches with zero action; all source and snapshot searches contain no generic start-pane affordance; stopped work names the exact consequence. |
| `ACP-VIE-006` | `PRD-VIE-006` | A, P | Inspector data exists only inside the Node View, previews remain bounded, and Session return restores exact layout/zoom/focus across navigation/reconnect. |
| `ACP-VIE-007` | `PRD-VIE-007` | A, P | Native snapshots at minimum/normal/maximum widths show icon/name/buttons left, metadata right once, and scoped operational status only at the bottom. |
| `ACP-VIE-008` | `PRD-VIE-008` | A, D, P | Session creation starts from its Workspace. End exercises `keep_processes`, `terminate` and `kill` independently: all three remove the Session row, the first sends no stop and preserves exact live runtimes by rehome/recovery, while only `detach_runtime_view` retains the row/lifecycle. One End/delete action under missing/stale/racing NativeJob, child, intent, MediaImport, CommitProposal+installation Attempt, BrowserDownloadQuarantine, TransferTicket, PortableExport, destination-linked PortableImport, relationship and Attention evidence always returns `closed`: the serial reducer discards uncommitted Turn drafts, revokes Session authority, applies one exact `ContainerSagaDisposition`, valid rehomes or recovery-inventory fallback and removes the row atomically. A graph/Attention/child mutation after request admission but before serial commit is included in the daemon-derived current vector and never yields stale-client retry. Pre-effect saga work cancels only with no-effect proof; generating proposal linkage, quarantine response/sealed-descriptor/ownership-transfer uncertainty, transfer temp/chunk/publication uncertainty and committing export/import output/remint identity survive without editor apply, redownload/copy, rewrite or reimport. Reservation fixtures admit exactly 4,096 recoverable subjects in one Workspace and 32,768 installation-wide, refuse each N+1 admission before effect, then End/delete at both full limits using or migrating existing ≤16-KiB/64-MiB/512-MiB slots without capacity refusal. The joint 10,000-Session+1,024-Workspace fixture pre-reserves exactly12,048 current ContainerCloseReceipt slots inside16,384/256 MiB; it proves Session close-or-delete and Workspace close→delete, while close→new Session(rearmed epoch)→close consumes only pre-reserved slots. Lost reply returns identical `closed` from the original fence and changed fingerprint conflicts. Under available minimal capacity, repeated empty close and a new operation on a tombstone reserve their own≤512-byte fence and return typed `closed_already`, never `ack`; same id replays and changed bytes conflict. Independent minimal count/byte N+1 returns `replay_capacity_refused_already_closed` only for that redundant new operation, with zero mutation and no required operator follow-up. Rich-receipt saturation refuses only future container admission; neither rich nor minimal saturation can block the first close/delete of an admitted container. Late same-subject evidence allocates nothing; unmatched runtime evidence goes only to `TargetRuntimeRecoveryInventory`; 180-day resolved compaction releases a semantic slot only after non-reuse fences remain. Unkillable/offline/racing cleanup produces bounded status/recovery evidence but can neither demand another user action, veto nor resurrect the row. |
| `ACP-VIE-009` | `PRD-VIE-009` | A, D, P | Search/group/board activation selects the same canonical Node/ViewTarget and co-attaches rather than duplicates work; an injected view panic leaves tree, transports and other views usable. |
| `ACP-VIE-010` | `PRD-VIE-010` | A, D, P | Installation-, Workspace- and ExecutionTarget-owned success/progress/warning/error fixtures obey priority, replacement, expiry, overflow/history and recovery rules through exact list/get/push revisions. Each owner admits 1,000 current events while the installation admits 100,000/256 MiB and each record is≤4 KiB; operation/producer reservation precedes effect. The 1,001st persistent error cannot evict another active error: N+1 producer admission refuses pre-effect, or an already admitted external producer uses its reserved gap while the source receipt survives. Crash before/after receipt/status projection, seven-day eligible compaction and stale owner cursors yield one event or explicit gap, never lost recovery. Live-region output announces start/terminal once and creates no Attention without a typed demand. |
| `ACP-VIE-011` | `PRD-VIE-011` | A, D, P | Table/board/search fixtures project the same canonical SessionId, AgentInstanceId or WorkItemId plus exact object revision and `ViewTarget`; mouse/keyboard selection and a card modal open the same `WorkSurface`, preserve one runtime/terminal stream and never mint a second identity or process. Session/Agent cards render only read-only runtime/status fields where no WorkItem mutation exists. A generated field-authority matrix proves drag/edit can change only the applicable WorkItem-owned state, priority, due, tag, comment or assignee field at its exact revision; stale/illegal/cross-kind operations conflict with zero mutation. Every projection action produces zero duplicate runtime, lifecycle, turn, dependency, start or Attention effect absent a separately configured demand. |
| `ACP-VIE-012` | `PRD-VIE-012` | A, D, L, P | Local create mints one Session/optional-Group WorkItemId+NodeId at backlog. External create reserves that pair plus CreationId/intent before dispatch; crashes before/after provider commit and receipt persistence reconcile only by proved correlation, never title/body/time or replay. Prepared cancel races dispatch with one CAS winner. Explicit import/rebind/detach and two concurrent same-key imports—including different Workspaces—yield one installation-wide owner and an authorised reference/refusal, retaining binding lineage/tombstones. Source CRUD and detach-versus-delete-local disposition emit zero provider mutation. Exactly64 non-deleted WorkItemSources split across several Workspaces are admitted; the65th refuses before credential/provider/configuration effect, and delete frees a slot only after bounded binding/mutation/conflict and resurrection fences survive. The installation independently admits1,000,000 small WorkItemKeyRegistry entries or exactly983,040 maximum≤512-byte entries/480 MiB, plus10,000 nonterminal-or-uncompacted source-operation slots; count,byte and operation N+1 refuse before Node/provider effect and are never asserted simultaneously. External create pre-reserves an unbound key slot, import reserves its exact key, and a tombstone folds only after terminal source deletion into a monotonic source-id/generation fence that rejects late events and id reuse. Terminal operation richness compacts after30 days only behind replay/key/source/conflict fences; nonterminal/possible-effect/reconcile/active-conflict evidence remains. Initial/paged sync, webhook/poll overlap, filter/mapping/credential/source generation rollover, cursor expiry, late old-generation input, daemon restart and compaction exercise every source/sync/coverage/freshness/backoff/presence state; only fresh complete exact-scope absence proves missing and only exact provider evidence proves source_deleted. All closed fields and three authorities are tested: local operations reject non-Turn fields, external edit/comment/assign/transition/close/reopen schemas do not overlap, fresh externally authoritative state may jump mapped states with provenance, reviewed merge retains both revisions, and echoed comments/assignees deduplicate by external identity. Mutation receipt before/after sync, possible-effect reconciliation, conflict versus newer remote revision, two conflict resolvers and detach/delete versus late page/webhook/receipt all have one exact outcome and no redispatch/resurrection. Archive/forget/restore/privacy delete retain the declared fence; active mutation/conflict evidence never compacts. Credentials never escape, and no projection/Attention/source-config operation closes/reopens/deletes provider work. |
| `ACP-VIE-013` | `PRD-VIE-013` | A, D, L, P | Product-announcement fixtures verify the exact `TURN-SIGNED-V1` preimage/RFC-8785 bytes and reject duplicate/unknown JSON fields, payload mutation, wrong domain/root/store revision/audience/channel/platform, expired/not-yet-valid/revoked/old epoch, rotation race, lower-sequence replay and same-sequence/different-hash conflict; same-sequence/same-hash is the sole idempotent replay and compaction/restart preserves high-water. Cross-domain valid command/update/voice signatures never verify. Oversize/superseded/dismissed/offline fixtures exercise the reducer. Sanitised bounded text and reviewed HTTPS links render accessibly; HTML/script/control/bidi/credential-bearing content is inert/refused. Historical evidence for a subsequently revoked announcement cannot reopen links or reactivate. Delivery/read/dismiss creates no StatusEvent, Attention, focus, command, setup consent or update-apply authority. Link open revalidates the current announcement trust store and pins envelope/payload/audience/link hash, preassigned Browser NodeId and explicit destination graph revisions; stale/selection/revocation races create nothing and a valid review creates exactly one isolated Browser Node. |
| `ACP-VIE-014` | `PRD-VIE-014` | A, D, L, P | A bijection covers every closed WorkItemActivity kind and matching typed delta; unknown/mismatched kind, >8-KiB delta and comment-body payload are rejected. Concurrent local/external state, metadata, comment-reference, assignee, import/sync, conflict, projection and source-delete mutations each append exactly one event keyed to WorkItem/revision/operation/source receipt with observed clock provenance. Total item-revision order ignores display timestamps; stable cursors, page gaps/resnapshot, external echo dedup, redacted author provenance, restart/compaction and permission scoping are deterministic. History selection routes the same WorkItem and emits zero runtime, source mutation or Attention effect. |
| `ACP-VIE-015` | `PRD-VIE-015` | A, D, P | A generated bijection freezes every hideable menu/header id, its slot and source→canonical-action relation and proves unknown, stale, duplicated, moved and hand-edited ids default visible. For every settings combination and minimum/normal/maximum viewport, snapshots and accessibility trees keep Attention, blocked/recovery affordances, destructive consequence access and Delete, Restart, Search and Close visible. Each hideable action disappears only from its optional presentation slot while the complete command palette remains keyboard navigable, focusable and invokes the same typed action/receipt; no per-action global shortcut is required. Mutation tests attempting to admit a critical id, move a menu id into the header set, hide all routes, alter authority/state/focus or make an action pointer-only fail. |

## Creation, flows and delegated work

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-CRE-001` | `PRD-CRE-001` | A, P | All four creation surfaces derive explicit placement subsets from the `category=creation` filter of one CommandCatalogue revision; their intersections expose identical action ids/names/defaults/validation/reasons, no independent CreationCatalog store exists, and grouping/order may differ. With zero Workspaces, installation scope exposes New/Open/Clone/SSH adoption. Session creation exists only in its exact Workspace row/menu, is absent from zero-state/global toolbar, and always binds that Workspace. |
| `ACP-CRE-002` | `PRD-CRE-002` | A, P | Default Session/Agent/Tool flows complete from Workspace + task/preset + optional prompt; only unresolved authority/collision choices interrupt. |
| `ACP-CRE-003` | `PRD-CRE-003` | A, D, P | Every Workspace create/duplicate and all six direct/template/read-only/worktree Session creation shapes plus Session duplicate carry a unique operation id, preassigned identity and exact owner/stream/target/catalogue/template/checkout revisions applicable to their kind. Duplicate clients, two windows, lost reply and daemon crash are injected before reservation, after reservation, during filesystem/worktree effect, after durable commit and before reply; retry returns the same identity/receipt and creates at most one Workspace, Session, CheckoutScope, worktree, Layout and bounded runtime reservation. A stale owner/template/target/checkout revision or occupied preassigned id changes nothing. Worktree variants prove canonical `non_primary`, unique isolated-worktree generation and lease before spawn; read-only variants prove enforcement; direct/template variants can never claim primary `main`. Multi-node creation likewise preassigns every child/Attempt and yields complete rollback, exact `WorkspaceSemanticRecoveryInventory`/`TargetRuntimeRecoveryInventory` evidence by subject kind or visible reconciliation—never an unobserved resource. |
| `ACP-CRE-004` | `PRD-CRE-004` | A, D, L | Built-in, signed-extension and local-operator catalogue entries traverse the same validation/adapter route. Signed-extension fixtures verify exact `TURN-SIGNED-V1`/RFC-8785 bytes and reject duplicate/unknown fields, payload mutation, wrong domain/root/store revision/audience, expiry, revoked/old epoch, rotation race, lower-sequence replay and same-sequence/different-hash conflict; same-sequence/same-hash alone is idempotent and high-water survives restart/compaction. Valid signatures from announcement/update/voice stores never cross-verify. Install, open, invocation and current-store/revocation races all revalidate; revocation disables before invocation. An extension contains only bounded data binding already registered typed operations and cannot contain code, executable/path fields, a new operation or capability. A valid operator-authored typed entry works; undeclared/custom capability, unregistered-operation and malformed-schema variants are rejected, while repository files and terminal/agent output cannot add or alter entries. |
| `ACP-CRE-005` | `PRD-CRE-005` | A | Template duplication contains no live/runtime/provider ids; Flow launch pins one immutable revision and later edits affect only later runs. |
| `ACP-CRE-006` | `PRD-CRE-006` | A, P | Single, multi, cancelled and cross-Workspace creates produce the specified selection/focus outcome; no child steals focus and no unsafe/missing owner receives input. |
| `ACP-CRE-007` | `PRD-CRE-007` | A, D, P | Clean/degraded/skipped setup discovers exact versions, guides auth/consent/trust at point of use, stores no credential canary and leaves generic terminal usable after every cancellation/failure. |
| `ACP-CRE-008` | `PRD-CRE-008` | A, D, L, P | New/open/clone/SSH-adopt each complete in one resumable catalogue path with exact target/path/repository identity; cancellation/crash at every phase reconciles once without duplicate repository or hidden residue, hostile saved config remains inert pending local consent, remote failure never opens a local namesake, and onboarding itself emits zero network publication. Installation ownership exists before a Workspace: 100 concurrent nonterminal onboardings are admitted, the 101st refuses before path/target/repository/network/Workspace effect, and a successful creation cross-stream commits exactly its preassigned Workspace. Terminal receipt saturation/180-day compaction retains replay proof while nonterminal/possible-effect/residual/reconcile evidence never ages out. A separate successful `publish_repository` fixture freezes exact hosted RepositoryAuthority, target/repository/destination/visibility/branch/upstream/credential revisions, canonical `non_primary` classification, Turn-owned isolated-worktree generation and active lease, then returns one durable idempotent receipt; lost response is resolved by lookup without republishing. Missing/stale authority or lease, primary-main alias/classification and changed destination/credential revisions fail before provider effect. At every onboarding or publish intent, phase, cancellation and recovery point, process/lease/lock/cwd scans find zero Turn-owned claim on the primary `main` checkout and an actual `git switch` succeeds there. |
| `ACP-CRE-009` | `PRD-CRE-009` | A, D, P | Toolbar, palette, context menu and shortcut project one contextual catalogue revision and stable ids for their permitted entries. A zero-Workspace installation query returns bounded New/Open/Clone/SSH entries under installation scope and no Session action; creating the first Workspace makes the old watermark stale and the Workspace-scoped query then exposes its row-only Session action. Eight CatalogueScans per connection and 512 installation-wide are admitted; each N+1 refuses without evicting a live evaluation. Final page, close, context invalidation, disconnect and exact 30-second idle expiry release it; reuse is gapped and reconnect cannot inherit it. Built-in, signed and local entry CRUD validates registered schema/capability and only local foreground operations can mutate local entries. Unicode/typo/prefix search is bounded, deterministic and content-independent; disabled/stale entries explain why. Shortcut fixtures cover built-in/signed/local, same-tier race, explicit named replacement, revoke and restart: a second viable binding without resolution leaves zero active binding and chord invocation has zero effect; explicit resolution yields exactly one named winner, and revoke never auto-activates a shadow. Invoking one entry resolves its one typed operation after current capability/revision revalidation, while unknown ids, output-injected labels and arbitrary command payloads execute nothing. Direct versus catalogue-wrapped credential, destructive and integration operations have identical effect class, role, LocalDesktopForegroundAuthority, review and capability outcomes across local, full-GUI remote, headless and Companion clients; the wrapper never upgrades authority. |
| `ACP-CRE-010` | `PRD-CRE-010` | A, D, L, P | A signed fixture matrix covers no-update, discover, bounded download, cancel/resume and separately verifies manifest plus exact streamed package envelopes. State/evidence bijection proves QueryOnly only for idle/no-update/discovery-failed, ManifestAccepted for available/downloading/downloaded, ReleaseAccepted from verified onward, and highest-phase evidence for later failed/discarded; deleting either required field or accepting any impossible pairing fails. Exact same-query discovery reuses the one current Installation intent; a different concurrent query and every second intent refuse before network. Declared package size is reserved before byte one; temporary/downloaded/verified/staged transitions share one allocation, reach exactly 2 GiB combined, and the next byte or allocation refuses without leaving partial state. Cancel/discard, crash and replacement prove bytes absent or retain the exact owner/chunk ledger; terminal replacement reserves/folds one of 100 receipts without dropping signing/anti-rollback/replay fences. Exact `TURN-SIGNED-V1`/RFC-8785 bytes reject duplicate/unknown fields, payload mutation, wrong domain/root/store revision/key/channel/platform/architecture/min-version, expiry, revoked/old epoch, rotation race, lower-sequence replay and same-sequence/different-hash conflict; command/announcement/voice roots never cross-verify. Package-to-manifest substitution, valid package with a different valid manifest, wrong/missing parent-manifest hash and package-byte mismatch never verify or stage. Manifest and package are both revalidated against current stores immediately before stage and apply; restart/compaction retains independent high-water, and code signing/notarisation is additional rather than substitutive evidence. Staging, explicit apply, restart evidence, failed apply and rollback-required exercise the closed reducer. The four exact RELEASE live-daemon/PTY cases derive the declared plan; only daemon-absent and compatible-daemon-preserve apply, while both incompatible cases replace nothing and never stop a daemon/PTY. Crash before/after replace, receipt and rollback boundaries reaches the phase-specific reconcile state; lookup proves applied/rollback-required/rolled-back/failed without repeating replace or rollback. Apply/rollback require LocalDesktopForegroundAuthority. Network/offline/update failure never blocks generic terminal work or installs silently, and logs/exports contain no credential or package body. |
| `ACP-CRE-011` | `PRD-CRE-011` | A, D, L, P | A local foreground review mints one expiring revocable Companion launch grant over exact principal/session, Workspace, Session, safe target/cwd, template, adapter and account revisions. The only accepted action uses that allowlist, preassigned Node/Instance/Attempt ids and one operation id; duplicate submission, lost reply, daemon crash and register/create ordering return the same durable receipt and materialise at most one hierarchy row and runtime. Unknown/stale/revoked/expired grant, changed catalogue/account/target/Session, arbitrary command/flag/cwd/checkout/provider fields, primary-main claim, remote fallback and cross-Workspace reuse refuse before filesystem, process or provider effect. Revocation fences queued work, while possible-effect recovery performs lookup/reconcile only and cannot duplicate, retarget or widen the launch. |
| `ACP-FLW-001` | `PRD-FLW-001` | A | Schema round-trip and invalid-case tests cover every declared node, role, command/prompt, dependency, context, execution, isolation, Attention and resource field. |
| `ACP-FLW-002` | `PRD-FLW-002` | A, D | A FlowRun survives reconnect/restart with immutable inputs, grants, operations, attempts, receipts/results and terminal state; definition edits do not alter it. |
| `ACP-FLW-003` | `PRD-FLW-003` | A, D | `FlowRunTrigger` accepts only foreground `manual` or `bounded_recurrence`: the former consumes one accepted preflight revision, while the latter consumes one exact occurrence id/instant/policy receipt, creates at most one new run and also passes the temporal oracle in `ACP-FLW-010`. `StepStartPolicy` accepts only `manual`, `with_run`, `after_success`, `after_result`, `all_of` or `any_of`: manual requires an exact foreground operation, with-run readies once when its run enters running, and only the four dependency variants consume current matching immutable typed DependencyResults. Producer-terminal or daemon-only verified-external publication first reserves the immutable result and policy-failure/recovery evidence before terminal commit, so no terminal source is unrepresented; only when the committed result set matches does a second atomic readiness transaction reserve `StepReadinessReceipt`, preassigned StepAttempt/RuntimeLaunchIntent and recovery capacity. A publish→ready→crash/replay fixture consumes one `ReadyStepDispatchEvent`, starts exactly one attempt and reconciles a possible launch only by lookup; duplicate dispatch, changed digest and a client-authored imitation start none. Fixtures independently fill4,096 results/32 MiB per run,100,000 results/256 MiB installation-wide and10,000 FlowOperationReceipts/run; every count/byte N+1 leaves explicit blocked/policy-failure recovery evidence and never permits a terminal producer with an unrepresented dependency signal. Idle/done/unknown labels, nonmatching predicates, later definition edits and duplicate/out-of-window occurrences produce no extra result, run, readiness receipt or StepAttempt. Publish→delete preserves and consumes the already immutable result once; delete→publish without a canonical terminal source produces exactly one `refused_impossible` decision that atomically materialises Blocked plus deduplicated actionable Attention, or enters the immutable `fail_run` reducer. Navigation never resolves that demand. Verified-external origin accepts only an internal daemon event derived from the immutable policy's exact canonical Turn receipt; forged/stale/cross-run/cross-edge/wrong-generation receipt and changed-fingerprint replay mutations publish nothing. Source-style persisted `--after` fixtures keep the visible edge/context relation but raw held shell commands, deleted-as-success and a bypassing “run now” cannot satisfy or replace the canonical policy. |
| `ACP-FLW-004` | `PRD-FLW-004` | A, D, L | An authorised conductor uses every allowed operation without another prompt; expired, over-limit, wrong-attempt, disallowed path/provider and self-expansion requests fail and route exactly once. |
| `ACP-FLW-005` | `PRD-FLW-005` | A, L | Independent reviewers fan out, the synthesiser cannot start early, every result remains inspectable and disagreement/missing evidence becomes exact Attention. |
| `ACP-FLW-006` | `PRD-FLW-006` | A, D, P | Flow writers receive unique worktrees/branches; primary `main` stays checked out and switchable in the operator checkout, and cleanup never removes data without explicit authority. |
| `ACP-FLW-007` | `PRD-FLW-007` | A, D | Fuzzed terminal/prose/escape output performs zero control operations; typed endpoint calls require capability and return one correlated durable receipt. |
| `ACP-FLW-008` | `PRD-FLW-008` | A, D, M, L | During large mixed-provider fan-out, create returns after receipts and a subsequent control request, terminal keystroke and Node route meet published budgets; Turn injects no join, while any provider-internal wait is labelled without stalling siblings/UI. |
| `ACP-FLW-009` | `PRD-FLW-009` | A, D | State-machine/property tests cover every legal/illegal transition and crash point; pause starts nothing, cancel revokes grants before dispositions, abort is fenced, retry creates one bounded attempt and all provisioning survivors reconcile. |
| `ACP-FLW-010` | `PRD-FLW-010` | A, D | Deterministic clocks exercise DST gaps/folds, backward/forward jumps, sleep/reboot, duplicate ticks, overlap and each missed-run policy; stable occurrence ids run at most once and bounds make backlog finite. |
| `ACP-FLW-011` | `PRD-FLW-011` | A, D | Every Flow/Grant/delegated-operation request and response round-trips the versioned schema; stale revisions/generations and duplicate ids fail or replay one receipt, and crash-at-effect-boundary resumes only from durable saga state. |
| `ACP-FLW-012` | `PRD-FLW-012` | A, D, L | A grant matrix creates/updates each allowed Resource kind and progress record once with exact provenance/receipts; wrong author/schema/kind/target/revision, byte/node/rate/expiry excess, delete/reparent/file mutation and content-as-control all fail before effect and deduplicate one expansion demand. |

## Agent topology and adapters

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-TOP-001` | `PRD-TOP-001` | A, L | Equivalent native events from every dedicated adapter produce the same source/epoch/scope/snapshot-or-delta envelope, parent and child attempts, causal operation, canonical lifecycle/turn facts and provider-only provenance. |
| `ACP-TOP-002` | `PRD-TOP-002` | A, D | Permutations, duplicates, delayed completion, reused ids and generation changes converge to one correct graph without cross-attempt mutation. |
| `ACP-TOP-003` | `PRD-TOP-003` | A, D | Process-first then structured and structured-first then process fixtures converge to one node; weaker reparenting cannot dislodge stronger provenance. |
| `ACP-TOP-004` | `PRD-TOP-004` | A, L, P | The Cartesian product of semantic/live/completed × direct/descendants × current-attempt/instance-lifetime matches an independent event manifest for proved-live, spawning, orphaned, conflicted, completed-with-no-active-attempt, nested, disconnected and unsupported cases; no oracle calls the production aggregate query and unavailable evidence never displays zero. |
| `ACP-TOP-005` | `PRD-TOP-005` | A, L, P | A PTY-less child streams bounded activity, timer/tools/tokens/result, survives view unmount/reconnect and retains completed history without a fabricated terminal. |
| `ACP-TOP-006` | `PRD-TOP-006` | A, D | At least three agent levels plus process descendants remain searchable/navigable through reconnect; persisted evidence restores honest completed/lost states. |
| `ACP-TOP-007` | `PRD-TOP-007` | A, L, P | Agent direct/descendant and Session/Workspace aggregates match queries at one graph revision and display coverage/confidence; no projection can disagree silently. |
| `ACP-TOP-008` | `PRD-TOP-008` | A, D, L | Complete empty snapshots alone produce exact zero; best-effort starts unknown, 1,025-event overflow/drop/disconnect/sequence gap immediately degrades exact to lower-bound/unknown, and a later matching asynchronous snapshot restores exactness without blocking input. |
| `ACP-TOP-009` | `PRD-TOP-009` | A, D, L | A total table-driven reducer maps every declared provider-native state, rejects unknown native values and illegal Lifecycle/Turn transitions, tests source-local ordering and comparable conflicts, correlates TurnId/revision/pending-result evidence and keeps turn completion, process exit and live children independent. |
| `ACP-ADP-001` | `PRD-ADP-001` | A | A repository guard rejects provider-name branching outside adapter packages/fixtures/docs allowlist; all behavior dispatches through the registry contract. |
| `ACP-ADP-002` | `PRD-ADP-002` | A, L | Every dedicated adapter passes the same complete state/topology/capability suite; differences appear only as supported/unsupported/degraded/unknown evidence values and no cell is omitted. |
| `ACP-ADP-003` | `PRD-ADP-003` | A | Unknown/custom/shell fixtures always select an adapter and expose only declared semantics with explicit unsupported/degraded reasons. |
| `ACP-ADP-004` | `PRD-ADP-004` | A, L | The capability manifest and test manifest are bijective; every live-dependent advertised cell links a passing dated versioned evidence record. |
| `ACP-ADP-005` | `PRD-ADP-005` | A, D | Hung/erroring usage, transcript and hook sources for one adapter leave other adapters, selection and terminal input within latency budgets. |
| `ACP-ADP-006` | `PRD-ADP-006` | A, L | Capability-selected launch/resume/branch/switch/stop succeeds for each claiming adapter; unsupported calls fail before side effects without daemon provider checks. |
| `ACP-ADP-007` | `PRD-ADP-007` | A, L, P | For each dedicated adapter, diagnostics show detected version, effective mechanism, fresh valid/rejected events and downgrade remediation; redacted export contains no canary secret. |
| `ACP-ADP-008` | `PRD-ADP-008` | A, D | A cross-product of adapter/version, account, host, endpoint and attempt/epoch proves stale or differently scoped facts cannot enable an operation; only current capability intersection plus authority succeeds. |
| `ACP-ADP-009` | `PRD-ADP-009` | A, D, L, P | Self-test preview/cancel/timeout/crash/live cases use disposable identity, enforce resource/quota disclosure, restore hooks, remove temporary resources and leave the inspected Session byte-for-byte unchanged with redacted receipts. |
| `ACP-ADP-010` | `PRD-ADP-010` | A, D, L, P | Exactly five independent Nodes share one live provider RuntimeEndpoint while retaining distinct daemon-minted BindingIds, provider thread ids, signature/key epoch, owner instance/attempt and endpoint generation across app, tmux and endpoint restart. The canonical HMAC fixture validates the exact domain separator, fixed/length-prefixed encoding, complete sorted 1..64 claims, 300-second expiry/30-second skew, candidate-stale-first transition and root/per-binding sequence high-waters; duplicate/unknown fields, missing/extra claims, replay, old/revoked epoch, wrong root/target/endpoint/generation and forged MAC leave all bindings stale or refuse before authority. With a valid root, five claims containing one unavailable/invalid/conflicted claim commit four current and one stale under one revision; the 64-claim form commits 63 current and one stale, proving one sibling cannot block the rest. Binding65 and every request/receipt count/byte N+1 refuse before mutation. Key rotation stales once, lost/locked keystore yields `continuity_unavailable` without remint, and crash/lost reply reconciliation never re-verifies into duplicate authority. Concurrent endpoint-unscoped→profiled rebinds CAS the global ownership registry to exactly one owner and move no transcript/index/cache/context/input bytes. Duplicate thread ownership, sibling operation, backpressure and explicit per-instance fallback converge without merge, cross-talk, identity remint or duplicate launch; fallback is always labelled rather than silently claiming continuation. Source-compatible fixtures deliberately carry no account field and must not create or satisfy an AccountProfile/CAP-032 claim. Where a separately authorised AccountProfile exists, its scope is orthogonal and mismatch still refuses without weakening the endpoint/thread binding. |
| `ACP-ADP-011` | `PRD-ADP-011` | A, D, L, P | Shared fixtures and one claiming live adapter exercise all nine normalized provider keys plus local adopt/cancel/projection/privacy operations. Two Workspaces race adoption of one incarnation-aware discovered key and create-vs-adopt: one Session/optional-Group Node owns it installation-wide, authorised replay returns that owner and every loser emits zero provider request. Create reserves Node+CreationId+intent before dispatch. For update/pause/resume/run-now/cancel-iteration/delete, crash fixtures at before-intent, prepared, dispatching-before-call, remote-effect-before-response, response-before-local-commit and local-commit-before-client-response prove one immutable MutationIntent per operation, lookup-only applied/not-applied recovery and zero redispatch; a write-only idempotency key advertises unsupported. Two simultaneous run-now intents map independently once to two iteration keys. Overlapping scans, broken cursor chains, partial/gapped/rate-limited pages, duplicate/out-of-order webhooks, generation change and stale post-tombstone events exercise complete-scan and schedule/iteration/presence reducers without false absence, terminal regression or reused-id resurrection. Exact external tombstone and correlated Turn delete reach provider_deleted; generic absence does not. Requested B versus last-proved A survives refusal, timeout, coercion and restart without calling B effective. One Job simultaneously materialises 1,000 independently cancellable active keys plus 100 terminal keys inside the 1,100 aggregate; every active key remains addressable, terminal 101 compacts only an eligible unreferenced terminal row, and active 1,001 from Turn refuses before effect while unpausable external excess produces a control-disabled coverage gap without overwrite. Oversize definitions/results and the 10,001st nonterminal intent enforce their own bounds. Refused/cancelled/not-created Nodes forget/restore/privacy-delete by CreationId; replay after privacy deletion/compaction hits its fingerprint fence with zero provider call, and sync cannot recache erased content before Restore. Attention before/during/after forget retains one id/route. Session/Workspace End/delete during every create/mutation/demand phase always removes the container in one action: it rehomes the same Node/Attention when valid or retains exact evidence and cleanup intent in the owning `WorkspaceSemanticRecoveryInventory` or `InstallationSemanticRecoveryInventory`, never drops evidence, asks for another decision or mutates provider work. |
| `ACP-ADP-012` | `PRD-ADP-012` | A, D, L, P | Versioned live and degradation fixtures run every common capability row for all six dedicated adapters and reject executable-name/generic fallback masquerading as dedicated support; two quota-only provider connectors report independent AccountProfile/target windows and activity states while every launch, transcript and control call remains absent/refused. |
| `ACP-ADP-013` | `PRD-ADP-013` | A, D, L, P | Two routes under one account remain isolated; late discovery after endpoint revision, 10k-model response, redirect/rebind/private-metadata/TLS change, missing/locked/revoked secret and wrong remote generation produce zero unintended request/launch/fallback. Secret canaries remain absent from argv/PTY/protocol/store/log/export; a valid launch receipt freezes route/model/mapping/credential generation, and a failed model switch leaves the prior attempt intact. |
| `ACP-ADP-014` | `PRD-ADP-014` | A, D, L, P | A cloned Workspace whose shared file says enabled remains disabled until a local foreground review adopts the exact capability revision; rejection changes nothing. A permitted AgentInstance/RuntimeAttempt creates an isolated logged-out Browser Node with immutable owner evidence, visible control badge and bounded typed navigate/read/click/type receipts. Wrong/stale instance or attempt, inherited/replayed flag, pre-existing/human/other-agent node, popup ownership ambiguity, filesystem/file URL, clipboard, credential store, authenticated partition, localhost/private/daemon/control origin and raw script/input routes all refuse before effect. Stop, revoke, expiry, owner termination and Workspace close fence queued/late operations; crash/lost reply reconciles one receipt without repeating interaction, and Browser content cannot widen the grant or hide control state. |

## Lifecycle, runtime and resources

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-LIF-001` | `PRD-LIF-001` | A, D, P | Two views attach/detach without changing process identity; one explicit input/resize owner exists, catch-up is bounded and competing bytes cannot interleave. |
| `ACP-LIF-002` | `PRD-LIF-002` | A, D, L | Table-driven tests prove §7 identity/conversation/process/data/reversibility with disjoint input contracts. Fresh Start and Branch pin destination, policy/source lineage and preassigned new Node/Instance/Attempt/launch identities, never an invented old handle. Resume pins the same Agent/Conversation plus either its latest terminal attempt or the exact committed adoption receipt's `no_prior_attempt`, a proposed-or-current binding, continuity proof and preassigned next/first Attempt. Domain `attach_runtime_attempt` pins an already-existing live/orphaned Attempt, reuse-safe handle/process-start, endpoint proof and binding/registry generations and returns `RuntimeAttachmentReceipt`; it may bind/reconnect that attempt but never launch/stop or create a Pane. Only live-runtime Restart, interrupt, terminate, kill, recycle and in-place switch pin the current handle/process-start, and each returns its declared typed intent/receipt. VNext presentation operations `attach_pane`, `resync_pane` and `detach_runtime_view` pin operation/Surface sequence, connection/Surface/PaneAttachment/attempt/binding/PTY/buffer generations, commit their exact closed attach/resync/detach `RuntimeViewReplayFence`, and change no domain lifecycle; detach returns `ack` without a lifecycle receipt. Same-operation replay returns the same variant, attachment identities and screen seal without a second CAS; changed bytes conflict. With the exact body live, attach and resync replay each return and verify the same body. After restart/eviction, either old seal returns explicit `body_unavailable`, every partial chunk is discarded, and the client runtime automatically uses a new operation id for attach or resync against current state, without operator action or a Start/retry control; a committed attach with only its body lost recovers by resync. Replacement races cannot attach the wrong Attempt, and count/byte saturation refuses attach/resync before replacing a view while an admitted detach always succeeds from its reserved fence. Stale/replayed/cross-owner and unsupported continuity refuse before effect. Restart stops once and starts replacement only after definite old-stop; Fresh Start alone creates an unrelated Agent identity; Tool Restart keeps its Node; terminate never escalates; Recycle cannot start before definite old-stop. Mutations that fabricate an old Attempt for Fresh Start/Branch/adopted-first-Resume, swap presentation/domain attach, admit any legacy alias after vNext negotiation (`detach_pane`, `close_pane`, `interrupt_node`, `terminate_node`, `kill_node` or `relaunch_node` with a resume flag), bypass/fold an exact view replay fingerprint, reuse an old seal for newer bytes, partially apply a chunked screen, swap `ptyRecycle`, `ptyTerminateForeground`, source `ptyKill` detach, force kill, Restart, Resume or Fresh Start semantics fail, as do reconciliation redispatch and count/byte N+1. None may substitute Session End/Delete or generic close. |
| `ACP-LIF-003` | `PRD-LIF-003` | A, D | Concurrent, lost-reply and post-timeout retries independently prove create/branch returns one durable launch receipt in the closed created/refused/uncertain result union; domain attach returns the identical durable `RuntimeAttachmentReceipt` in its closed attached/recovered/refused/uncertain result union for the same pre-existing Attempt; and Resume from a terminal attempt or committed adopted `no_prior_attempt` returns one next/first Attempt receipt. Launch crash points cover prepared, dispatching, process-created-before-receipt, submitted-unconfirmed, reconcile-required and receipt-before-reply; get/reconcile is lookup/probe-only and never spawns. Interrupt has an independently durable prepared-before-signal receipt and covers signal-before-reply/restart/uncertainty without a second signal. The shared launch/lifecycle/configuration/interrupt rich pool reaches100,000 small bundles or exactly65,536 maximum8-KiB bundles/512 MiB, and its minimal pool independently reaches1,000,000 small fences or exactly983,040 maximum512-byte fences/480 MiB; every count/byte N+1 refuses before effect, terminal richness compacts after180 days, and possible-effect evidence does not. Same-AttemptOwner competing lifecycle operations and same-AgentInstance competing configuration switches use distinct operation ids: exactly one may own the nonterminal slot, every loser refuses before signal/provider effect, and independent per-owner/per-instance=1 plus installation=10,000 and N+1 fixtures prove no second stop, replacement or configuration epoch. Each operation id creates at most one runtime or binding mutation, changed fingerprints conflict, and lookup/probe reconciliation never launches, stops, signals or backend-attaches twice. Presentation `attach_pane` is outside this creation receipt family. |
| `ACP-LIF-004` | `PRD-LIF-004` | A, D, P | A recovery matrix independently exercises UI reload, daemon/shell restart, host reboot, local/remote reconnect and event-source loss with declared survivors. |
| `ACP-LIF-005` | `PRD-LIF-005` | A, D | Background metadata restore, child/resource/history selection and stale plans produce zero launches; verified durable handles attach; only a persisted still-valid Flow step or ACP-LIF-009 foreground activation can start once. |
| `ACP-LIF-006` | `PRD-LIF-006` | A, D, P | End/delete races every child and every one of the frozen 26 SemanticRecoverySubject kinds, including NativeJob create/mutation/projection, runtime launch/lifecycle and live-node effect slices, WorkItem create/mutation/external projection, FlowRun children, context-packet/agent-message writes, MediaImport, CommitProposal+Installation Attempt, repository publish, WebPreview, Browser create/navigation/quarantine/action, print, bulk/eco/companion, transfer and portable destination state. The separately generated denominator classifies all 108 durable Workspace families exactly once as a typed bundle member or deterministic exclusion; missing/duplicate/ephemeral/default family, unconditional all-Node eligibility, relationship catch-all, child allocation where inherit is required and source+target double-slot transfer mutations fail. Every saga race maps to `cancel_pre_effect`, `terminalise_no_effect`, `rehome`, `workspace_recovery` or `installation_recovery`: cancellation needs no-effect proof. Reservation follows the destination predicate: a launch into a Node that already owns `node_aggregate` inherits it, while any launch whose destination lacks that reservation allocates before identity/spawn and transfers one-to-one when its preassigned Node becomes live. Thus inert saved descriptors, session activation, runtime-node origin, Fresh, Branch and each new Flow child are covered without an origin-list gap; the FlowRun keeps only its coordinator reservation, an existing-Node Flow step inherits that Node, and no Flow child inherits the Flow slot. Each bulk-restart child likewise inherits its own Node reservation and never the bulk coordinator. A Companion child inherits its parent slot. Runtime-effect replay stays independent. Native/WorkItem mutations inherit their projection slot; create→projection, launch/companion→Node and quarantine→ticket preserve one ReservationId/fingerprint lineage. Late evidence cannot publish into a tombstoned container, resurrect a Node/Session or restore input authority. Presentation park/wake/detach retires or wipes, grants/links/leases revoke or use their own fence, Attention reroutes, deterministic relations become history and OS/helper uncertainty uses TargetRuntimeRecoveryInventory or ProcessCleanupCharge. Proposal/attempt linkage, delivery write correlation, transfer publication, export output and import remint identity survive without redispatch, copy or reimport. The total reducer discards drafts, revokes Session authority and consumes/moves only pre-existing paired Workspace/Installation reservations; exact4,096/64-MiB Workspace and32,768/512-MiB installation caps and each N+1 prove subject admission refuses pre-effect while End/delete at full capacity performs zero reservation INSERT and never refuses. Schema11→12 migration fixtures cover zero/exact/N+1 count and bytes, live/orphaned/reconnected/spawning versus inert/terminal rows, stable external Agent identity, malformed enum/JSON/UTF-8, duplicate fingerprint, dangling owner/provenance, legacy Attention/primary lease and crashes before/after DDL, each candidate, paired inserts, marker, user_version and commit. Each result is exactly untouched reopenable schema11 or complete schema12 bijection/digest; failure exposes bounded diagnostic plus backup/offline-export guidance and performs no restore/process/file/network effect. Re-run is byte-idempotent and mismatched registry digest blocks write-open. Recovery summaries page at the declared bounds, cursors gap on revision change, and one consolidated receipt-filtered API verifies all semantic/runtime/cleanup roots and routes every row in one action to its exact redacted WorkSurface; no snapshot must contain all 32,768 entries. Unkillable/offline process, worktree, provider, file, artifact or receipt cleanup cannot veto. Session End with `keep_processes` still removes the Session row: same-checkout rehome atomically transfers the lease owner+generation, otherwise a closed ended-Session owner tombstone preserves the host-lock/process-start fence without authority or dangling foreign key; exact `release_workspace_write_lease` reconciliation can release only after quiescence, and Workspace delete transfers the pre-reserved Installation checkout fence. Count/byte N+1 refuses a future writer acquisition, never End/delete. Only `detach_runtime_view` retains row/lifecycle. Provider/user-data destruction remains separately reviewable/refusable. |
| `ACP-LIF-007` | `PRD-LIF-007` | A, D, P | Both complete §7 event×entity tables generate tests for same-generation UI reload, client replacement, daemon/shell restart, reboot, disconnect, accepted/refused remote reconnect, source loss and destroy; every Node/instance/attempt/conversation/runtime/PTY/receipt/packet/message/Attention/grant/link/lease cell varies independently and matches exactly. |
| `ACP-LIF-008` | `PRD-LIF-008` | A, D, L, P | Complete/partial/gapped target-wide snapshots reconcile known, unknown and survivor handles; exact adopt creates one typed owner/attempt, ignore is revision/expiry scoped and exact terminate cannot touch a sibling, other host or same-named local runtime. |
| `ACP-LIF-009` | `PRD-LIF-009` | A, D, P | From a visible safe Session, one pointer/keyboard selection restores Layout, attaches live work and starts the exact bounded eligible saved descriptor set—or exactly one configured default Shell when empty—with no secondary action. Before spawn one operation id persists a deterministic descriptor→preassigned Node/Attempt reservation map; two windows, duplicate request, lost receipt and crash at every reservation/spawn/commit boundary return the same per-reservation receipt and create at most one attempt each. Stale policy/revision, changed target/account/cwd/command, missing containment, ambiguous survivor, permission or unsafe owner launches nothing and exposes one exact consolidated recovery action. |
| `ACP-LIF-010` | `PRD-LIF-010` | A, D, L, P | A mixed inventory pins candidate and policy revisions, previews the exact bounded restart set and excludes working, waiting/blocked, background, missing-session, unsupported-resume and stale-generation instances before effect. One confirmed operation processes eligible instances sequentially, each through its own preassigned restart/resume intent and lookup-only idempotent reconciliation; duplicate request, cancel, crash/lost reply at every instance boundary and reconnect never duplicate a stop or attempt. The final ordered summary accounts once for every candidate as restarted, skipped with reason, failed or reconcile-required; a changed candidate is not substituted, terminal input and Attention remain responsive, and no primary checkout or unrelated instance is touched. |
| `ACP-LIF-011` | `PRD-LIF-011` | A, D, L, M, P | With Eco disabled, no runtime hibernates. With it enabled, an exact candidate/policy revision admits only idle off-screen local resumable agent CLIs and excludes working, waiting, blocked, unknown, recurrent, live-subagent, background-task, remote and non-resumable fixtures. A deterministic clock proves no more than two exits in any minute. Before exit, one durable intent pins Session, Attempt, conversation, tmux/scrollback, Attention and resume evidence; crashes/lost replies before and after exit reconcile idempotently without a duplicate stop or resume. The tree and view expose hibernating state, returning to the runtime wakes and reattaches automatically with no extra action, and wake failure stays recoverable without dropping Attention, context or continuity evidence. |
| `ACP-RUN-001` | `PRD-RUN-001` | A, D, L | Local plus one remote backend pass the same create/attach/resize/input/signal/observe/close contract and return location-bound handles. Target-catalogue fixtures preassign monotonic ids and exercise create/adopt/probe/trust/rotate/bind/unbind/retire/delete with same-operation replay and crashes before/after local commit/receipt. Exactly 256 non-deleted targets and 10,000 operation slots are admitted; each N+1 refuses before descriptor, connection, trust or binding effect. Delete frees the live slot only after all references resolve, folds identity into non-reuse/fingerprint/trust fences and 180-day terminal compaction cannot reactivate or substitute a same-named host. |
| `ACP-RUN-002` | `PRD-RUN-002` | A, D, P | App restart reattaches the same process; host reboot performs only declared cold reconstruction/resume and labels lost process continuity explicitly. |
| `ACP-RUN-003` | `PRD-RUN-003` | A, D, L | DNS/host/generation/path changes and outage refuse the operation with scoped stale state; no local process/path is opened as fallback. |
| `ACP-RUN-004` | `PRD-RUN-004` | A, M, P | Shell, alternate-screen TUI, service and log fixtures pass every applicable `TERMINAL_ACCEPTANCE.md` case—PTY, resize, IME, clipboard/path drop, safe links, scrollback, bounded find next/previous/wrap/no-match, keyboard and lifecycle—and display no invented agent facts. Find pins one buffer revision, sends zero PTY bytes and changes no selection/lifecycle/Attention. Boundary fixtures stop before 1,000,001 cells or 100,001 logical lines, label coverage bounded instead of false no-match, resume only from the exact same revision/cursor, cancel within one ≤25 ms work slice and preserve terminal-input/Attention latency under saturation. Text-document fixtures analogously stop before the byte after 16 MiB and reject invalid UTF-8 boundary cursors. Eight searches per Surface and 512 installation-wide are admitted; each N+1 refuses without evicting another live search. Explicit close, source invalidation, Surface loss and exact 15-minute idle expiry free the pin; reuse is stale and reconnect cannot inherit it. A separate residency fixture reaches 128 terminal states with exact 2-MiB rings and 4-MiB current-grid reservations, then refuses the 129th pre-spawn unless a stopped/unpinned/checkpointed state is released. It independently reaches the 8-MiB screen/512-MiB family, 5,000-row, 16-payload/16-MiB image-store/512-MiB family and 12-payload/12-MiB client-cache/256-MiB family bounds under the shared pool; pressure trims only old unpinned scrollback or non-authoritative image caches, reports gaps/placeholders/refusals, preserves current cells/text/input/Attention and never evicts a live PTY. Image-pipeline fixtures race 128 partial sequences and decode bombs while independently saturating eight 8-MiB scans, eight 8-MiB multipart assemblies and two complete 128-MiB measured high-waters; excess consumes only discard-state, one visible refusal and zero extra decoder bytes. Success leaves only≤4-MiB verified RGBA. |
| `ACP-RUN-005` | `PRD-RUN-005` | A, D, P | Restore/delete of each resource kind performs zero file/network/external-open/content execution; only a typed foreground action may load or mutate its Turn record. |
| `ACP-RUN-006` | `PRD-RUN-006` | A, D, L, P | Local/remote explorer begin mints a scan id/pinned directory revision and continuation requires exact page sequence/predecessor digest; watches start only from complete scans. Sixteen scans per connection and 1,024 installation-wide are admitted; each N+1 refuses without evicting another live scan. Final page, explicit close, invalidation, disconnect and exact 60-second idle expiry release the pin; later continuation is gapped and reconnect cannot inherit it. Client-chosen/reused scan ids, symlink/hardlink/mount/TOCTOU, cursor gap, rename/delete race and outage never escape or fall back. Selecting and opening one Explorer row freezes the same FileBackend target/root/generation and regular-file identity into `open_file_for_edit`; ACP-RUN-010 saves only that identity, while target outage, stale generation and a same-named local file produce no fallback or cross-target edit. A credential-free repository with no remote exercises status/diff/stage/unstage/local-commit/branch/conflict/discard/worktree cleanup under exact filesystem RepositoryAuthority; no profile, grant, credential lookup or network occurs. Hosted fixtures add exact profile/grant and cover commit-and-push/fetch/pull/push; filesystem authority refuses every network verb. Both bind repository/CheckoutScope/canonical-classification revisions. Every mutation matrix includes primary working-tree, symlink, mount, path alias, resolved index/lock and primary-branch targets and proves refusal before file/provider effect; only enforced read-only reads may inspect primary main, and a Turn-managed non-primary writer also proves its isolated-worktree generation and lease. Full-GUI remote can call only ordinary stage/unstage/commit/fetch/branch members; commit-and-push/pull/push/conflict-resolution/discard/cleanup are absent and LocalDesktopForegroundAuthority-only, headless is read-only and Companion has none. Commit-history fixtures return a bounded stable DAG with parent ids and separately paged exact changed-file identities/statuses per commit; graph cycles, missing commits, stale repository revisions and oversized pages fail/read-gap without mutation. Each mutation preassigns and persists one RepositoryMutationIntent before effect; crash/lost reply at every boundary and duplicate operation exercise lookup-only reconciliation over exact objects/refs/index/worktree/remote correlation without repeating Git or network work. Commit-and-push independently covers all definite/ambiguous local-commit×remote-push products. Exactly 10,000 intents within 256 MiB are admitted; each count/byte/receipt/journal/recovery N+1 refuses pre-effect, while 180-day compaction retains replay/object/ref/scope/lease/correlation fences and never removes possible-effect evidence. Every effect has an exact repository receipt and destructive review. |
| `ACP-RUN-007` | `PRD-RUN-007` | A, D, P | A matrix covers direct/Template/Flow Session, Agent, Tool and Pane create plus activate/restore/resume/restart/recycle/switch/branch, attach/adopt and legacy migration; `chdir`, absolute paths, symlink/alias/mount escape and `git -C` writes fail continuously, uncontained adoption is unmanaged, guarded read-only is classified separately, and the final scan finds zero primary-main write leases/processes/registrations/locks while `main` remains switchable. Registry fixtures admit exactly 100,000 live-or-uncompacted CheckoutFence records within 64 MiB and 100,000 lock inodes; each N+1 writer/worktree/lock admission refuses before filesystem, repository, process or lock effect. Crash at every reserved/held/releasing/terminal boundary and same-operation replay proves lookup-only reconciliation, globally non-reused fence/lease generations and no duplicate writer. A malicious same-name inode, stale pid, elapsed-time sweep and busy live lock cannot release or remove anything; only a durable released terminal plus exact ownerless exclusive-lock and fresh no-writer inventory permits inode sweep, while 180-day compaction preserves replay/reference/non-reuse fences. |
| `ACP-RUN-008` | `PRD-RUN-008` | A, D, L | MITM, wrong/stale host key, replay, revoked credential, key-rotation race and log/export canaries fail before effects; one real remote path proves authenticated encryption and no raw credential persistence. |
| `ACP-RUN-009` | `PRD-RUN-009` | A, D, L | Runtime, file and repository contract suites reject cross-capability use; wrong host/generation/root/revision and remote outage produce one refusal/uncertain receipt and zero mutation of a same-named local path/repository. |
| `ACP-RUN-010` | `PRD-RUN-010` | A, D, L, P | Local/remote text opens and atomic saves cover external edit conflicts, retry/merge, encoding/size/permission/offline states plus symlink/hardlink/mount/TOCTOU/root escape; no failed save overwrites bytes, falls back locally or mutates via terminal/Resource-only edit. Each save preassigns FileSaveIntentId and reserves active/temp/terminal capacity before byte one. Crashes at prepared/writing-temp/temp-sealed/replacing plus lost reply and same-operation replay use only exact descriptor/before/after/temp proof to reach applied, conflict-no-replace, failed-no-replace or retain uncertainty; reconcile performs zero write/rename/delete/retry and unsafe temp sweep removes nothing. Exactly 256 active intents and 2,048 MiB combined temporary bytes are admitted with each file≤8 MiB; every count/byte/50,000-terminal-slot N+1 refuses before a temp/file effect, and 180-day folding preserves replay/generation/hash evidence while possible-replace never ages out. |
| `ACP-RUN-011` | `PRD-RUN-011` | A, D, L, P | WebPreview restore/preview executes no script/form/navigation/download; Browser fixtures cover address/history/reload, isolated partitions, reviewed popup/quarantined download and explicit storage clear, while ambient credentials, daemon origins, file/symlink/mount escape, localhost host/port/generation change, DNS rebinding and content-as-control all fail before authority or external open. Popup crash/lost-reply/retry before and after intent/load/publication yields exactly its preassigned Browser Node or proved absence without a second load. Local-HTML fixtures reserve before descriptor read, admit exactly 32 memory snapshots/256 MiB with each≤8 MiB, refuse each count/byte N+1 without read, permit no sibling load and prove bytes vanish on navigation/close/scope/process loss. Download response bytes bypass the renderer; exactly 32 BrowserDownloadQuarantines within the shared 4,096-MiB transfer pool and 10,000 terminal slots are admitted, and each count/byte/receipt/recovery N+1 refuses before body byte one. Every reducer transition, exact 30-minute expiry, cancel/Node-close race and crash before/after sealing or ownership transfer preserves descriptor/hash/charge evidence. One sealed review atomically moves descriptor+charge into one Browser-source→reserved-File-Resource TransferTicket without copy/redownload; ticket-cap refusal leaves quarantine intact, wrong response/size/type/hash never publishes, and lookup-only reconcile proves exact quarantine-or-ticket ownership without duplicate Node/blob/network. |
| `ACP-RUN-012` | `PRD-RUN-012` | A, D, L, M, P | A capability/type matrix proves WebPreview, Browser and Media are three non-substitutable Node/view classes. Drop and paste valid image/video fixtures through prepare/read/validate/committing/commit with a preassigned Node/blob reservation; streamed paste exercises indexed ≤4-MiB chunk reorder/duplicate/changed-replay/gap/backpressure, total/hash, 8-MiB frame cap and stale client/session generation. MIME spoof, polyglot, oversize, symlink/hardlink/mount/TOCTOU, decoder bomb, unsupported codec, duplicate hash, cancel and crash before/after every state transition produce the declared receipt with no partial visible blob. Crash/cancel around publication reconciles only the sealed temp/blob/Node binding, never rereads or recopies; proved absence cancels/fails, proved publication commits and ambiguity remains reconcile-required. Separate Node identities may share refcounted bytes. Restore never decodes/autoplays; exact-revision play/pause/seek/mute/volume/caption fixtures cover duration unknown, bounds, stale generation, codec/error and track removal while affecting only sandboxed playback, and content performs zero network/file/script/clipboard/input/control operation. |
| `ACP-RUN-013` | `PRD-RUN-013` | A, D, L, P | Create/adopt/external-auth/validate/rotate/revoke/delete exercise one RepositoryHostProfile per exact host/target credential reference. Independent hosted-RepositoryBackend and WorkItemSource grants each prove their own id, profile/target/trust/host/account/credential revisions, repository/project scope, expiry and active-to-revoked-or-expired reducer. Wrong kind/origin/account/scope/generation, N+1 capacity, expiry/revoke races and secret canaries fail before hosted effects; every external WorkItemSource and hosted RepositoryAuthority reject missing/stale/mismatched profile+grant fences, while filesystem-only RepositoryAuthority performs local Git with no host-profile lookup. Revoking/regranting one seam never reuses its old id or changes the other. Reads expose only safe identity/state, and profile delete atomically revokes grants without removing provider data or an externally owned credential. |
| `ACP-RUN-014` | `PRD-RUN-014` | A, D, L, P | Exact repository/staged-index revision and bounded diff hash feed only the frozen CommitProposalProviderProfile revision; each durable Attempt precedes dispatch and records the executable/broker descriptor hash, sandbox-policy revision and numeric limits. A generated state table covers prepared→dispatching/succeeded/every closed failure reason; Proposal generating and Attempt dispatching begin atomically, and ready+succeeded or failed/expired+failed end atomically. Cancel/expiry at every dispatch edge kills/fences work, leaves no nonterminal Attempt and exposes no reconcile/redispatch operation. Boundary fixtures admit 64 profiles, current plus 31 revisions and 10,000 Attempts; each N+1 create/update/generate refuses before current-state change, helper spawn or broker request. Referenced revisions, every nonterminal Attempt and a terminal Attempt whose Proposal is nonterminal survive compaction; only a terminal Proposal+Attempt older than 30 days with minimal replay proof frees its slot, and same-operation retry cannot evade the count. Oversize/binary/secret/control data is omitted/redacted with receipt. Executable fixtures prove a fresh empty non-repository cwd, allowlisted environment without HOME, only fds 0–2, one sealed canonical stdin, no inherited socket/credential, PTY, clipboard, keychain, daemon/control or repository/workspace/home/arbitrary-filesystem access, no network, at most 4 processes, 512 MiB RSS, 30 s wall/10 s CPU and 8 KiB each stdout/stderr; exactly two helpers reserve the full 1,024-MiB family/shared pool, the third Attempt waits with zero process, and a hung cancellation remains ProcessCleanupCharge-owned/charged until OS proof without admitting another helper. Broker fixtures permit only the exact pinned ModelEndpointProfile seam and no helper network. Repo/home/credential/socket/extra-FD/child-flood/network canaries and unsupported sandbox enforcement fail closed before useful output. Timeout, OOM, process/output flood, crash or ambiguous provider effect terminalises that Attempt, kills its process tree and never respawns it under the same operation id. Stale profile/index results never apply. Output is sanitised/bounded, apply changes only the commit-message editor draft, and process/network/repository scans prove zero stage/commit/push/branch/file mutation. |
| `ACP-RUN-015` | `PRD-RUN-015` | A, D, L, M, P | Upload/download tickets cover every local/remote/client-stream source×destination pair with distinct endpoint target/root/trust/descriptor or client/session/surface generations, chunk reorder/duplicate/changed-replay/loss/backpressure, pause/resume, expiry/cancel from every nonterminal state, disconnect/crash, wrong size/hash/direction and destination conflict. Every overwrite-policy variant is unrepresentable except `create_new`. Descriptor and symlink/hardlink/mount/TOCTOU attacks fail; temporary bytes are owner-only, hash verification precedes atomic commit, completed output is non-executable, and cancel/expiry racing publication resolves to completed only for proved output or to its requested terminal only for proved absence. Ambiguous completion stays reconcile-required and lookup by ticket neither duplicates transfer nor falls back to a same-name local/remote path. |
| `ACP-RUN-016` | `PRD-RUN-016` | A, D, L, P | Terminal snapshot, Note and editor switch plain↔Markdown at one exact source revision and restore selection/focus. HTML/script/event handlers, remote/local images, unsafe links, bidi/control injection, oversize/decode failure and unsupported binary remain inert or explicitly unavailable. Source bytes/hash, terminal input sequence, file revision, network counters and Attention stay unchanged; later source revisions never rewrite the pinned projection silently. Exact reviewed link open pins Projection/source/link hashes, preassigned Browser NodeId and explicit destination graph revisions; stale/change/selection-race fixtures create no Node/network action and a valid request creates exactly one isolated Browser Node. |
| `ACP-RUN-017` | `PRD-RUN-017` | A, D, L, M, P | Local and remote fixtures bind image/PDF reads to exact FileBackend target/root/generation/descriptor/revision and admit only declared bounded MIME/size/page geometry. Image and multi-page PDF render in an isolated non-editing ViewTarget; page navigation, zoom, bounded text search and reviewed print mutate no source, selection owner, terminal, Browser, Media, Attention or control state. Revocable object URLs are per-view, disappear on close/revision/target loss and cannot be reused; stale/offline, wrong MIME, polyglot, symlink/hardlink/mount/TOCTOU, corrupt/oversize/encrypted/decoder-bomb and print-cancel fixtures yield a labelled refusal or gap with no local fallback, network fetch, script, clipboard or file effect. Memory/page/decode concurrency reaches declared limits and N+1 refuses without blocking terminal input. |
| `ACP-RUN-018` | `PRD-RUN-018` | A, D, L, P | Explicit keyboard/menu copy, paste and path drop bind the current Surface, selection/input-lease revision, user gesture and bounded UTF-8/path manifest; stale focus/lease, background, oversize, secret/path-policy, remote-target and cancellation cases produce zero clipboard or PTY bytes. Clipboard payload never enters logs, store, sync, context or Attention. OSC 52 fixtures from local, remote and alternate-screen programs attempt read, write, fragmented, encoded, oversized and nested sequences: every direction is swallowed/refused before clipboard access and produces no response bytes, while ordinary terminal output remains faithful. Duplicate reviewed paste/drop uses one input receipt and cannot replay after reconnect. |
| `ACP-RUN-019` | `PRD-RUN-019` | A, D, L, P | A generated bijection exposes exactly checkout-commit, delete/rename-branch, force-push, init, merge, rebase, revert and stash push/pop as typed requests with closed schemas; unknown verb, raw argv/config/env, unsafe ref/path and injection payloads execute nothing. Each operation pins exact RepositoryAuthority, non-primary CheckoutScope/worktree lease, status/ref/HEAD/upstream/remote and credential revisions applicable to its kind. Primary-main, dirty/stale/unpublished ambiguity and cross-repository/target requests refuse. Delete, force, rewrite, detach and overwrite consequences require one exact foreground preview/review. Crash/lost-reply fixtures at intent, fetch/inspect, command subphase, conflict, remote effect and receipt commit reconcile by proved Git/provider state without blind redispatch; conflicts and partial effects remain visible, retries are idempotent, and no operation changes the operator's primary checkout or cwd. |
| `ACP-RUN-020` | `PRD-RUN-020` | A, D, L, P | Default-off and opt-in fixtures pin BrowserNodeId, SurfaceId, Browser revision, visibility revision and exact hidden duration: `5m−1` never discards and `≥5m` may discard only when loading, audible, live agent-control lease, popup, download/quarantine, print, reviewed action and dirty/unsubmitted-form predicates are all false at the same revision. Every suppressor and its race with the timer is mutated independently. A successful reclaim visibly replaces the renderer with a discarded state, ends its process/resource charge and preserves only the safe canonical public URL/history descriptor with an explicit back/forward and form/POST-state-loss label. Reselection requires no operator action: it durably reserves one fresh `BrowserNavigationIntent`, revalidates the existing reviewed public-navigation policy and idempotently rehydrates exactly once; crash/lost-reply/reconnect at prepare, renderer creation, navigation and publication reconciles by intent lookup without a second load. Stale/revoked policy, non-public URL, localhost, local-file, credential/storage substitution, popup/download/print/action/form uncertainty and capacity N+1 each create no renderer/network effect and expose one exact blocked reason in the view/status path, never `Start pane`. Saturating the existing Browser, navigation-intent, process-memory and recovery-evidence limits refuses before allocation; discard releases only proved renderer resources, retained descriptors remain charged to their Browser Nodes, and recovery/compaction never loses a possible navigation effect. |
| `ACP-RUN-021` | `PRD-RUN-021` | A, D, L, P | Local macOS, remote target, unsupported and measurement-failure fixtures bind each reading to ExecutionTarget/generation, used, ceiling, required headroom, measured-at, coverage and freshness. Exact boundary tests at `<80%`, `80%`, `ceiling-headroom-1`, `ceiling-headroom` and literal exhaustion emit healthy/elevated/critical state as declared; the critical edge is the safe-headroom boundary, not equality with exhaustion. Warning plus deduplicated Attention is committed before the first capacity-refused spawn receipt, stale/unknown readings never claim healthy capacity, and pressure/remediation never kills, detaches or reaps work. Resource accounting proves bounded one-minute sampling, five-minute reminder deduplication, target isolation and no terminal-input/Attention starvation under many targets. Privileged remediation requires one foreground review pinning target, current kernel setting, proposed value, command/provider identity and consequences; intent is durable before elevation. Cancel, wrong target/generation, stale before-value, unsupported privilege broker and N+1 receipt capacity cause zero mutation and precise safe guidance. Crash/lost reply at request, elevation, persistent-config write, reread and rollback reconciles only by exact before/after/provider evidence without rerunning; partial apply is visibly uncertain or rolled back, never reported fixed from hope. Platforms lacking this full receipt/reconcile/rollback contract expose no auto-fix and provide bounded target-specific manual remediation. |
| `ACP-RUN-022` | `PRD-RUN-022` | A, D, L, P | A generated matrix covers selected/visible/off-screen time, local/remote, tmux-backed/plain-shell, current/stale attachment+attempt+handle generations, working/waiting/blocked/unknown/idle, Attention, viewer/relay watcher, input lease and output/backpressure state. A view switch may create one warm renderer Park immediately; it expires by five minutes, twelve fit exactly and the thirteenth is not admitted until the oldest quiescent renderer is evicted, while protected/nonquiescent work remains charged to its existing attachment rather than becoming a thirteenth Park. Independently, `10m-1` retires no attachment; at `>=10m` only a proved durable unwatched client may release its viewer/PTY charge. Memory/PTY pressure cannot widen detach eligibility, and the ten-minute zero-watcher safety path only reconciles that detach and never ends a durable Session. Shadow/background-write fixtures retain exact session identity, bounded output ordering, input lease/safety and backpressure with zero PTY device. Selection/approach, Attention routing and current input automatically reattach/repaint the same runtime exactly once with no process respawn, `Start pane` or second action. Crash/lost reply/reconnect before and after warm eviction/detach/reattach reconciles by exact handle and reports retained/released renderer, viewer, PTY, durable-session and process resources without duplicate attachment or hidden work loss. |

`ACP-RUN-011` applies the same BrowserNodeCreationIntent reducer to inert create, popup, ContentProjection link
and announcement link. It independently admits exactly512 nonterminal intents,10,000 small total records or
8,192 maximum≤4-KiB records/32 MiB, then mutates each active/count/byte/terminal/journal/recovery setting plus
100,000 small or98,304 maximum≤512-byte/48-MiB installation-lifetime
replay fences independently; every N+1 produces zero graph,
renderer or network effect. Navigate/back/forward/reload/stop exercise every BrowserNavigationIntent edge and
crash before/after dispatch. Lookup-only recovery proves applied/no-effect, retains reconcile-required or writes
terminal dispatched-unconfirmed after irrecoverable evidence loss; same-operation replay never dispatches.
The suite fills256 active navigation intents,100 rich receipts/Node,10,000 rich receipts/128 MiB and100,000
minimal replay fences. It independently saturates eight≤256-MiB renderers within1,024 MiB,100 history
entries/Node,10,000 small entries or8,192 maximum≤8-KiB entries/64 MiB, and≤128-MiB partitions within512 MiB. Direct, popup,
link and redirect fixtures exercise BrowserUrl at 4 KiB/2,048 scalars plus one byte/scalar N+1 and title at
1 KiB/256 scalars plus N+1; oversize input dispatches nothing, oversize redirect follows no further hop and
commits no history, ten redirects succeed and the eleventh follows no further hop/commits no history, and
oversize title becomes typed omission rather than a prefix. Each count/item/family/shared-byte N+1 loads nothing; history eviction
cannot remove the current or intent-targeted entry; clear, navigation-away, Node close, scope/renderer loss and
reconnect prove the declared byte/release behavior. A renderer that ignores termination remains charged through
ProcessCleanupCharge; closing/reopening eight owners admits no ninth renderer, while quiescence/OS reclamation
releases exactly once and never resurrects its Node/Surface authority.
It also fills16 current+pending BrowserPages at32 KiB/512 KiB, proves a third generation for one renderer and
the17th global record dispatch nothing, success replaces current only after old references quiesce, and stop/
failure/Node/partition/renderer/daemon loss discards pending. DOM/body canaries appear only inside the existing
renderer/partition allocation; a hung renderer transfers, rather than releases or duplicates, the page slot/
bytes until OS proof.
The BrowserLocalSnapshot state-table separately injects cancel/stop/open failure before read, read/hash/
descriptor drift, replacement and Node/scope/owner/renderer/daemon loss from every legal source state. Each
case reaches terminal discarded once, frees both family and shared-memory charge and makes the old id/bytes
unrecoverable; deleting any discard edge or leaking either charge fails `ACP-RUN-011`.

The same row treats `load_web_preview` as the sole WebPreview network path. It accepts only a foreground-owned
Surface and one exact Node/source revision plus canonical private HTTPS URL≤4 KiB/2,048 scalars, then exercises
every `prepared→fetching|cancelled|refused`, `fetching→loaded|failed|reconcile_required` and lookup-only
`reconcile_required→loaded|failed|fetch_unconfirmed|reconcile_required` edge. Crash/lost reply after fetching
never performs DNS or HTTP again. Ten redirects, 8 MiB transferred, 16 MiB decoded, 20:1 expansion and 30 seconds
are independently saturated; each N+1 closes without a partial renderer. The suite admits exactly 32 live load
states, one/Surface and four/connection across at least eight connections; 16-MiB/item/256-MiB bodies; eight 64-MiB/256-MiB inert renderers; 32
nonterminal intents; 10,000≤4-KiB rich receipts/64 MiB; and 100,000 minimal replay fences. Every count, byte,
shared-RSS, receipt, journal and recovery N+1 refuses before DNS/request/read/render. Close, source or ViewTarget
change, Node/Surface/connection/renderer loss and exact 15-minute idle expiry race before DNS, headers, every
body boundary and terminal receipt. Each atomically fences/stops the exact correlation, reaches
`failed(closed_no_result)` only after socket/worker/buffer quiescence proof or retains intent+all charges in
reconcile-required; lookup never refetches. Repeated close/disconnect/reopen still admits only 32 and the N+1
proves zero orphan socket, worker, buffer or renderer. Restore, background selection and reconnect load nothing.
The same32 states each reserve one≤8-KiB WebPreviewFetchCorrelation under the exact256-KiB family cap before
DNS. The33rd/item-byte N+1 performs no request; every close/source/ViewTarget/Node/Surface/connection/renderer/
expiry race retains the correlation slot+bytes through socket/worker/buffer uncertainty, transfers it once to
ProcessCleanupCharge when ownership disappears and releases only on quiescence/OS proof. URL/header/body
canaries remain absent from correlation metadata.
`web_preview_changed` carries only the exact owning
state/revision and bounded progress/outcome, never URL path or body.

For `ACP-LIF-006` and `ACP-VIE-008`, “every saga” explicitly includes BrowserNodeCreationIntent and BrowserNavigationIntent.
Prepared work cancels only with no-effect proof; dispatching/reconcile-required/dispatched-unconfirmed identity,
partition, renderer token and possible-load evidence rehomes or consumes its pre-reserved recovery slot before
the container row disappears. Late evidence may close only that retained intent and can neither publish into a
deleted Session nor redispatch network/history/reload/stop.

It also includes WebPreviewLoadIntent. Prepared load cancels only with proved zero request; fetching,
reconcile-required or fetch-unconfirmed evidence rehomes or consumes its pre-reserved recovery slot before row
removal. Presentation authority disappears atomically; proved-quiescent allocations release, while each
possibly live socket/worker/buffer plus its charge transfers to recovery-owned cleanup until quiescence and
cannot veto End or escape the same caps. A late receipt can terminalise only the retained
intent and can never render into, publish under or refetch for a deleted Session.

`ACP-RUN-012` independently fills 32 nonterminal MediaImports, one 10,240-MiB Workspace physical temporary+
blob pool and 10,000 terminal receipts. Each count/item/Workspace-byte/receipt/recovery N+1 refuses before
descriptor read or first chunk. Crash before/after temporary seal, blob ownership/refcount transfer and Node
publication proves one charge and no copy/double count; ambiguous publication keeps bytes/evidence charged,
while 30-day terminal compaction preserves Node/blob/operation/refcount replay fences.

The same `ACP-RUN-012` run admits one MediaPlaybackState per Surface, four per connection and 32 installation-
wide, with≤64-MiB decoder working sets inside a 512-MiB family aggregate and the shared variable-RSS pool.
Count/family/shared-byte N+1 and failed same-Surface replacement preserve the old state and perform no decoder
spawn/read. Exact state/revision controls cover first play, pause, seek, mute, volume, caption selection, stop,
ended, error and pressure park. Codec/container hits 64 ASCII bytes and N+1; every closed error enum member plus
unknown free text, 64 caption tracks plus N+1, id 64 bytes/32 scalars, BCP-47 35 bytes, all three kinds, label
128 bytes/64 scalars and complete encoded state 32 KiB plus N+1 prove exact accept/refuse behavior. Oversize or
unknown input preserves the current state and emits no decoder/control effect. Stop/ended/error plus source/
blob invalidation, ViewTarget replacement and Node/Surface/connection loss fence the exact decoder and request
termination but retain its slot+family/shared bytes until descriptor/process/thread/shared-buffer quiescence or
OS-reclamation proof. Hung/ignored termination remains cleanup-pending and charged; repeated connection cycling
cannot admit a 33rd decoder, End/delete still returns from the existing recovery reservation, reconnect inherits
no authority and no case autoplays or mutates the Media Node/blob.

`ACP-RUN-013` additionally exercises authenticate and rotate crash boundaries before reservation, at prepared,
after dispatch commit, after provider effect, after provider response and after local commit. Same operation/
fingerprint returns one RepositoryHostCredentialIntent; changed fingerprint conflicts; get/reconcile performs
provider lookup only. Rotate atomically enters degraded(rotation_pending) and revokes all active grants before
provider effect; correlated success reaches validating, explicit validate alone reaches active and zero grants
auto-reactivate/regrant. Refused/not-applied remains degraded until old-generation revalidation, revoke/delete
fences late callbacks and no-correlation providers are unsupported. Exactly one nonterminal/profile, 10,000/
64-MiB credential intents, 128 active grants/profile and 100,000/256-MiB terminal grants saturate independently;
each N+1 has zero broker/provider/profile/grant effect and 180-day folding preserves all declared high-waters.

## Context, communication and telemetry

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-CTX-001` | `PRD-CTX-001` | A, D | Destination/current-attempt broker reads only allowlisted sources/bounds; expiry, revoke, reconnect and stale generation fail closed and leave an audit. Fixtures independently fill 64 links/endpoint,10,000 active links, one≤4-KiB bearer per current destination attempt/10,000 bearers/32 MiB and four read buffers/attempt,16/Link,256 installation-wide at 1 MiB each/256 MiB plus the shared pool. Every item/count/family/shared/30-second N+1 opens no source, dispatches no helper and consumes no link budget. Pre-commit failure releases after I/O quiescence; post-commit disconnect keeps the disclosure charge but releases the body only after the sole destination write quiesces. Direct Archive races against new links/in-flight reads under exact graph/authority revisions: it atomically suspends each link and revokes bearers before hiding while retaining Attention routes. Restore issues a fresh bearer only after full revalidation and performs no read; End/delete permanently revokes and never reactivates. Attention arriving while archived routes to the exact temporary WorkSurface without unarchive or launch. |
| `ACP-CTX-002` | `PRD-CTX-002` | A, D | Packet preparation/delivery preserves immutable provenance/body+framing hash/review+redaction state and distinguishes queued/submitted/received/uncertain/failed; crash fixtures prove one-off review loss and no hidden body replay. Direct cross-Workspace delivery/ContextLink is refused. Portable export emits only package-local packet/artifact ids and inert content; import remints ids, strips every runtime/operation/grant/host identity and cannot deliver until a distinct fresh destination review succeeds. |
| `ACP-CTX-003` | `PRD-CTX-003` | A, D, L, P | Oversized native transcripts yield bounded older digest + exact recent tail + separately gated artifact; receipt states complete/budgeted/partial and omissions. Export/import round-trip preserves declared content hashes/omissions while hostile source ids, authority fields and executable payloads are rejected or stripped and never satisfy a dependency, resume work or bypass destination budgeting/review. Export ownership remains its exact source Workspace; import remains Installation-owned and one atomic revision vector either creates the preassigned new Workspace or remints children beneath the explicit existing Workspace/Session/Group. Fixtures fill 16 export and 16 import slots plus the shared 2-GiB temporary reservation with exact≤64-MiB packages; each N+1/count/byte attempt refuses before read/write/remint. Cancel, daemon crash, external output race and destination deletion prove exact written/not-written/committed/not-imported/reconcile outcomes without duplicate write/import; only proved byte absence releases capacity. Ten-thousand-receipt saturation and 30-day compaction retain minimal replay/collision fences, while committing/reconcile evidence never ages out. |
| `ACP-CTX-004` | `PRD-CTX-004` | A, D, P | An authorised Flow creates declared context with no extra dialog; out-of-scope sources produce one consolidated exact review and cancellation provisions nothing. |
| `ACP-CTX-005` | `PRD-CTX-005` | A, L | Native branch/continuation preserves verified ids; unavailable support visibly becomes portable handoff with new identity and correct lineage, never silent resume. |
| `ACP-CTX-006` | `PRD-CTX-006` | A, D, L | AgentMessage FIFO/TTL/capacity/idle/input-owner/generation cases use a structured endpoint and yield correlated queued/submitted/received/read/acted/expired/refused/unconfirmed states with no duplicate delivery or PTY fallback. Fixtures independently hit body4 KiB,16 unaccepted drafts/connection,256 items/1 MiB per destination,10,000 small live bodies and exactly8,192 maximum bodies/32 MiB, the64-MiB body+queue+encoder family/shared RSS,600-second TTL and100,000 terminal slots plus every N+1 before write; count and body bytes are never asserted simultaneously. Queue acceptance transfers ownership to daemon+Workspace+delivery+destination generation; source disconnect preserves it, destination disconnect remains bounded queued, and expiry/terminal/daemon-loss release only after buffer quiescence or OS reclamation. |
| `ACP-CTX-007` | `PRD-CTX-007` | A, D | Only a valid typed result/revision satisfies an acyclic edge; idle/exit/prose cannot; authorised downstream starts once and other nodes stay blocked. |
| `ACP-CTX-008` | `PRD-CTX-008` | A, D | A cross-product authority test proves every edge/message/context body grants zero undeclared context, execution, checkout, focus or approval rights. |
| `ACP-CTX-009` | `PRD-CTX-009` | A, D | Foreground root issue/update/renew/expand/revoke plus valid `submit_delegated_operation` exercises succeed and retain issuer/grant/generation; every agent call to a direct context/packet/message endpoint and every widening/wrong source/destination/expired/post-revoke exercise fails before disclosure and routes one exact demand. |
| `ACP-CTX-010` | `PRD-CTX-010` | A, D, L | Descriptor/root TOCTOU, symlink/hardlink/mount, remote MITM/replay and control-framing fixtures fail closed; canaries in paths/files/transcripts/env/diagnostics never enter unauthorised packet/store/log sinks. |
| `ACP-CTX-011` | `PRD-CTX-011` | A, D, L | Every declared/forbidden BodyAuthority×Transport×Evidence combination, transition, byte/count/TTL/input gate and crash boundary matches both closed machines. ContextPacket independently hits 1-MiB body+1-MiB review, 16 drafts/connection, 128 bodies/256-MiB family+shared RSS, 600-second TTL and 10,000 metadata slots; every item/count/family/shared/metadata N+1 reads/assembles/provisions/launches/installs/writes nothing. Acceptance transfers ownership to daemon+Workspace+delivery+target generation, source disconnect preserves the saga and quiescence/daemon-loss proves release. Pre-write body loss terminates, all eight post-submit evidence combinations remain independent, late exact evidence may reconcile unconfirmed without writing, overflow refuses, PTY is never used and ambiguity cannot retry. |
| `ACP-CTX-012` | `PRD-CTX-012` | A, D, L, P | Pinned Note reads return only the exact revision; reviewed-live reads accept only allowed authors/schema/revisions within cumulative budgets, audit each returned revision and surface consumers, while edit races, budget reset, redirect, revoke/read race and non-Note source fail without leakage. Four parallel reads on one attempt and16 on one Link reserve cumulative budget before source access; the next request, a 1-MiB+1 byte body, a 257th installation buffer and a response after 30 seconds all fail before disclosure, and revoke wins every not-yet-committed race. |
| `ACP-CTX-013` | `PRD-CTX-013` | A, D, L, P | Bounded complete/partial/gapped inventories across two providers, profiles, targets and endpoints page/search without cross-profile cache or false absence; global ConversationKey uniqueness rejects duplicate current ownership and similarity never binds. Adoption preassigns Node/AgentInstance/`proposed` endpoint-binding/receipt, freezes the complete descriptor plus Session/tree/profile/grant/target/adapter/endpoint/global-registry revisions, and atomically creates exactly one stopped identity with explicit `no_prior_attempt`; its binding is exactly `proposed` with zero provider, launch, input, transcript, context or Attention authority. The exact committed adoption receipt enables only a separately preflighted `resume_agent_instance(no_prior_attempt)` to create the first RuntimeAttempt and promote the binding after continuity proof; `attach_runtime_attempt` remains invalid until a real existing attempt is independently inventoried/proved. Later terminal-attempt Resume follows the normal route. Concurrent owner, stale/gapped evidence and crash before/after commit yield zero partial identity or the identical terminal receipt; get/reconcile performs lookup only. Independent rich-receipt fixtures reach 100,000 count with small rows and exactly65,536 maximum4-KiB rows/256 MiB, while minimal replay fixtures reach1,000,000 count or exactly983,040 maximum512-byte rows/480 MiB. Every count/byte N+1 refuses before the ownership CAS; rich compaction after180 days retains the exact committed-or-refused fence, including `no_prior_attempt`, same-id replay is identical and changed bytes conflict. The UI Resume route emits only `resume_agent_instance` for an already owned stopped Agent; an unowned row offers Adopt and adoption never auto-resumes. |
| `ACP-OBS-001` | `PRD-OBS-001` | A, L, P | Launch/switch/resume views and receipts show requested/effective/current values, warnings and evidence for every claiming adapter with secrets absent. |
| `ACP-OBS-002` | `PRD-OBS-002` | A, L, P | Context and quota fixtures with conflicting percentages/windows render separately with correct labels, scopes, units and reset semantics. |
| `ACP-OBS-003` | `PRD-OBS-003` | A, L | Multiple accounts/providers/hosts update independent rows and scopes; shared allowances never appear as per-agent facts. |
| `ACP-OBS-004` | `PRD-OBS-004` | A, M, P | Runtime view and tree status expose the exact lifecycle/turn/task/child/age/resource/unread facts at the required revision and pressure state. |
| `ACP-OBS-005` | `PRD-OBS-005` | A, P | Unknown/unsupported/stale/rate-limited/fetch-failed fixtures have distinct accessible labels and cannot render a numeric zero/current value. |
| `ACP-OBS-006` | `PRD-OBS-006` | A, D, M | Bounded independent collectors cancel/timeout/cache correctly; hidden views stop expensive polling and selection/input latency stays within budget. |
| `ACP-OBS-007` | `PRD-OBS-007` | A, D | Canary secrets in argv/env/output/provider errors are absent from protocol, UI snapshots, store, logs, diagnostics and exported telemetry. |
| `ACP-OBS-008` | `PRD-OBS-008` | A, D, L, P | Two providers, hosts and accounts exercise create/adopt/external-auth/validate/default/retire/delete plus concurrent launches; precedence and LaunchReceipt freeze are exact, defaults affect only future work, active references block delete, sibling reads of auth/config roots are denied by the declared sandbox/broker, unsupported isolation refuses cross-profile concurrency, and credential/config/transcript/conversation/quota/fallback never crosses profiles. |
| `ACP-OBS-009` | `PRD-OBS-009` | A, D, L, P | Capability cross-products independently enable/disable title read and rename; read never writes, rename uses exact ConversationKey/provider revision and one idempotent receipt, and stale/timeout/ambiguous/rate-limited/unsupported cases never change the displayed effective title without correlated provider evidence. |
| `ACP-OBS-010` | `PRD-OBS-010` | A, D, L, M, P | Local/remote complete-empty, nonempty, partial, gap, collector error and stale target fixtures render distinct coverage and never substitute local/other-host or false zero. Independent process-tree accounting covers PID reuse, cycles, overlapping/shared trees, current and closed Session owners plus unmatched survivors without double count; exact fresh termination touches only its target+generation+handle+start identity and argv/env canaries never escape. |
| `ACP-OBS-011` | `PRD-OBS-011` | A, D, P | Declared/provider/generated/manual/fallback name facts follow precedence and revision; a pinned alias survives reconnect and provider changes, same-cwd siblings never cross-name, stale/oversized/secret/control/bidi/multiline proposals fail, Group proposals use bounded summaries, and local apply emits no provider rename, command or terminal byte. |
| `ACP-OBS-012` | `PRD-OBS-012` | A, D, L, P | Default-off fixtures read and persist zero transcript bodies. Opt-in indexing binds every fragment/hit to exact provider endpoint, mandatory AccountProfile, target/generation, ConversationId and transcript revision; endpoint-unscoped input is unsupported and reads/indexes zero body bytes. Its encrypted generation explicitly contains postings plus the final≤200-KiB normalised segment tail/document inside the same≤512-MiB profile/target and≤1-GiB installation caps and key-revocation/redaction/delete lifecycle; body canaries are found only in that private index or its authorised request page and nowhere in logs, diagnostics, export, sync, telemetry, context, Attention, clipboard or another scope. Count/byte/file/query/time/result/retention N+1 refuses or truncates with explicit coverage, and malformed JSONL, oversized text, secret/redaction canaries, missing/deleted/changed files, account retirement, target loss, partial scans, cursor gaps, crash and rebuild expose exact freshness/coverage/error rather than empty success. Deletion removes every tail/posting/snippet copy and preserves only bounded non-resurrection fences. Selecting a current sealed hit reserves HistoricalConversationViewBuffer, first≤64-KiB indexed-tail page and outbox/chunk capacity before CASing exactly one Surface to the closed read-only `historical_conversation` ViewTarget; N+1 leaves the old Surface/query byte-identical, while a committed selection returns the reserved page in the same response and keeps the tree selection only as navigation origin. Cursor pages preserve exact predecessor/index/source/ViewTarget coverage and never reopen provider files. Concurrent index delete/profile retire/source loss automatically CASes back to that current hierarchy target or Workspace overview and drops late pages with bottom-status reason, never blank/Start. Forged ordinal/page digest, stale/late Surface or ViewTarget revision, changed index/source and cross-profile/target seals give one precise refusal; no case mints a Node/owner/grant or emits adopt, resume, branch, launch, context, Attention or terminal input. |

`ACP-OBS-012` independently saturates one nonterminal operation/scope, eight installation-wide, 10,000 rich
receipts and their exact8-KiB/64-MiB/180-day boundaries, then the permanent 1,000,000-count and983,040-maximum-
item/480-MiB minimal-fence boundaries. Every N+1 refuses before transcript read or key/index effect. Historical
views independently saturate one buffer/Surface,16 installation-wide and2-MiB/item/32-MiB with a30-second
deadline; selection/query/page/outbox admission is atomic. Disable/delete, profile/target retire and privacy
delete revoke the exact current key generation, erase every indexed body, view/page buffer and outbox copy, and
retain only the body-free operation/replay fence needed to prevent resurrection.

`ACP-OBS-008` additionally crashes at auth pre-reservation, prepared, dispatch-before-call, provider effect
before response, response before local commit and commit before client response. Same operation/fingerprint
returns one AccountAuthenticationIntent; changed fingerprint conflicts; get/reconcile performs correlation
lookup only and never relaunches. Correlated success moves only to validating, explicit validation alone reaches
active, and retire/revoke/delete plus a late callback cannot reactivate. Exactly one nonterminal/profile,
10,000 intents/64 MiB, 64-MiB per-root and 2,048-MiB aggregate root capacity are saturated independently;
every N+1 refuses before broker/Browser/provider/root effect. A broker lacking correlation or write quota is
unsupported, and 180-day folding retains all declared replay/generation/correlation fences.

`ACP-OBS-009` additionally runs owned-with-current-attempt, owned-without-live-attempt and proved-unowned
subjects; unowned is refused whenever the global registry has an owner. It crashes at prepared, after dispatch
commit, provider effect, provider response and local commit. Same operation/fingerprint returns one
ConversationRenameIntent; changed fingerprint conflicts; cancel is prepared-only and reconcile performs
correlation lookup without redispatch. An unrelated same-title provider change cannot resolve the intent.
Correlated resolution alone supplies effective title/new revision, while every uncertain/refused path leaves
the provider observation and local/pinned alias unchanged. Exactly one nonterminal/key and 10,000 intents/64
MiB saturate independently; N+1 has zero provider/alias effect and 180-day folding retains replay fences.
Titles at 512 UTF-8 bytes/200 scalars pass, each next byte/scalar, multiline/control/bidi and missing-correlation
adapter fails pre-effect; the latter advertises local alias only.

## Attention and voice

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-ATT-001` | `PRD-ATT-001` | A, D, L | Each structured demand routes to exact semantic subject, current attempt, interaction/result and verified owner; stale/mismatched pairs are refused. |
| `ACP-ATT-002` | `PRD-ATT-002` | A, D, L | Ordering/property tests cover the closed actionable type set, safety class, age/policy ties and independent lifecycle/turn/unread/pressure/quota axes; 100 actively running agents with no demand, result, failure, pressure or quota trigger produce zero queue entries, while each completed result produces one exact TurnComplete/unread-result route. Independent boundary fixtures fill the 200,000-entry count cap with smaller records and the 768-MiB byte cap with exactly 196,608 maximum-size≤4-KiB active entries; separate fixtures do the same for the 200,000-entry/768-MiB terminal-receipt pool. Count and byte saturation are never asserted simultaneously. Every producer reservation and N+1 refusal occurs before spawn/subscription/provider effect, materialisation consumes rather than duplicates its reservation, unresolved entries never compact and exact dedup alone replaces. An unpausable producer cardinality overrun consumes only its pre-reserved actionable observability-gap slot, preserves underlying evidence and exposes no fabricated response. Restart/180-day compaction retains route/subject/replay fences. |
| `ACP-ATT-003` | `PRD-ATT-003` | A, P | Shortcut, badge, desktop notification, HUD and permitted automatic focus resolve the same route/revision and land in one interaction. |
| `ACP-ATT-004` | `PRD-ATT-004` | A, D, L | Navigation/read/ack/resolve mutate only their named axis; submission remains pending through disconnect until exact provider completion evidence. |
| `ACP-ATT-005` | `PRD-ATT-005` | A, D | Heuristic/fuzz evidence can only badge/propose; only focus-worthy structured evidence reaches the governor, including after replay/reconnect. |
| `ACP-ATT-006` | `PRD-ATT-006` | A, P | Every sensitive/user-active state defers and later applies/expires the original route; queue, badge and unread revision remain intact; manual navigation wins. |
| `ACP-ATT-007` | `PRD-ATT-007` | A, D, P | Parent turn transitions and new turns do not clear a live/completed child's node, status age or unread result; only exact result rendering marks it read. |
| `ACP-ATT-008` | `PRD-ATT-008` | A, D, P | Reconnect/cross-device clients converge on one queue revision; each companion action is capability-gated and sensitive actions require declared surfaces. |
| `ACP-ATT-009` | `PRD-ATT-009` | A, D | Dedup, ageing/no-starvation, snooze/new-revision wake, dismiss-without-resolve, mute-with-badge, cooldown/manual-route and every policy inheritance field match `ATTENTION_ACCEPTANCE.md`. |
| `ACP-ATT-010` | `PRD-ATT-010` | A, D, P | Node-less/owner-less evidence opens the exact ProvisionalAttentionView; no Node/input owner is created or borrowed, and later binding/stale revisions cannot redirect or submit the original route. |
| `ACP-ATT-011` | `PRD-ATT-011` | A, D, L, P | Local/full-remote/Companion typed questions/decisions use exact `respond_to_agent_interaction`; stale/wrong/permission ids fail and only provider evidence resolves. Every RuntimeAttempt reserves eight≤8-KiB Workspace PendingInteractions and their Attention routes before spawn. A 10,000-live-Attempt fixture therefore reserves exactly 80,000/625 MiB PendingInteraction tokens and 90,000/351.5625 MiB Attention tokens before any spawn, then materialises all eight prompts plus every gap by consuming those tokens with no second charge. The installation caps are 100,000/768 MiB PendingInteractions and 200,000/768 MiB Attention, leaving 20,000 count slots/143 MiB—only 18,304 further maximum-size PendingInteractions—and 110,000 count slots/416.4375 MiB Attention headroom. Separate fixtures reach the PendingInteraction byte cap with exactly 98,304≤8-KiB records and the Attention byte cap with exactly 196,608≤4-KiB records; count and byte saturation are never asserted simultaneously. Count/byte/partition N+1 and 100,000 terminal-receipt saturation are exact. A distinct ninth is backpressured or yields the reserved typed-response-disabled gap without overwriting a prompt; terminal 180-day compaction preserves id/route/option/claim/replay fences and live/possible-effect records never age. For permissions, fresh typed local desktop, two remote clients/grants and verified-local-PTY race one installation-unique PendingInteraction under the full Installation/Workspace/ExecutionTarget authority vector: one attempt/route-scoped claim wins and every loser emits zero bytes/provider requests. Two concurrent issues to the same client/session exercise GrantIssueKey; issue-vs-claim has one winner. Crash fixtures before claim, after prepared, after durable effect_armed, during/after provider call or PTY enqueue and before/after receipt commit force the declared dispatch/evidence pair, generation-change reconciliation with zero redispatch and a retained tombstone. Sensitive class, session expiry, raw-byte bypass, unsupported/degraded/unknown transport, remote PTY, widening, offline/cross-profile/secret paths fail. N+1 nonterminal/terminal capacity refuses before effect with bounded system Attention; Attention closes only on provider evidence. |
| `ACP-ATT-012` | `PRD-ATT-012` | A, D, L, M, P | Background/killed-client, duplicate replay, presence leave, offline, expiry, 401/403 and revocation-during-batch fixtures enforce endpoint/grant generations, minimal E2EE payload, exact subject/revision collapse and bounded retry; two same-titled subagents yield two demands, accepted/failed delivery never resolves either, deep links resync, resolve/read/delete emits a terminal live event and late ticks cannot resurrect it. NotificationHostMode packet/listener proof finds zero public listener or inbound port. |
| `ACP-ATT-013` | `PRD-ATT-013` | A, D, M, P | Deterministic audio fixtures distinguish `done` and `needs-you`, start within 300 ms of the canonical state edge and remain non-blocking under simultaneous terminal input and 100 concurrent agents. Mute emits zero audio; min/max volume and per-subject cooldown are exact, duplicate/replayed/stale edges do not sound, and settings survive restart without autoplay. Missing/blocked audio support is labelled and leaves the visual, screen-reader and structured Attention paths complete. Playback creates, routes, acknowledges and resolves zero Attention records, changes no focus/unread/provider/runtime state and carries no task text or secret. |
| `ACP-VOI-001` | `PRD-VOI-001` | A, D, P | Network denial, remote Session and worker crash/hang/OOM prove PCM/inference stay local and Sessions/Attention/runtimes continue unaffected; the packaged worker has no outbound socket route. Boundary fixtures admit one MicrophoneLease/DictationTarget, two exact 10-MiB PCM reservations, one≤32-KiB hypothesis and one≤512-MiB live-or-cleanup SpeechWorker under shared RSS; every third-buffer/second-worker/item/family/shared N+1 opens no microphone/spawns nothing. Capture/inference stop at300 seconds, shutdown waits two seconds, and a hung tree stays ProcessCleanupCharge-owned/charged until OS proof without blocking window close. |
| `ACP-VOI-002` | `PRD-VOI-002` | A, D, P | Selection/attempt/prompt/window changes never retarget a capture; zero target bytes precede explicit reviewed Insert/Send and uncertain delivery never replays. A completed result occupies the existing one≤32-KiB LocalInputDraft/Surface plus≤4-KiB voice metadata, with eight/client,64 drafts/2 MiB and64 metadata/256 KiB installation-wide. An existing draft blocks capture rather than being replaced. Same-window daemon reconnect preserves visible text but disables delivery until explicit fresh retarget review; Surface/window/client exit drops it and old ids/generations never reactivate. |
| `ACP-VOI-003` | `PRD-VOI-003` | A, D, L, P | The voice-model manifest verifies exact `TURN-SIGNED-V1`/RFC-8785 bytes in only `SigningTrustStore(voice_model_manifest)`. Duplicate/unknown fields, payload mutation, valid command/announcement/update signature, wrong store revision/audience/platform/engine/model, expiry, old/revoked epoch, rotation race, lower-sequence replay and same-sequence/different-hash conflict fail before download or native parsing; exact replay is idempotent and high-water survives restart/compaction. Bad artifact digest/size/redirect/symlink/TOCTOU/race also fails before parsing; store revocation between download and load revalidates and refuses. A verified model works offline and inventory/export/delete accounts for all artifacts without private signing keys. |
| `ACP-VOI-004` | `PRD-VOI-004` | A, D, P | Spoken control/approval words, permission focus and Attention changes cannot invoke control, choose approval, retarget draft or mutate queue state. |
| `ACP-VOI-005` | `PRD-VOI-005` | A, D, P | Sandbox-denial probes prove no daemon socket/repository/credential/arbitrary-filesystem/network access; CR/LF/control/bidi inputs sanitise, and PCM/hypothesis/draft canaries are absent from protocol/store/journal/log/diagnostic/crash/export sinks. |

`ACP-ATT-012` additionally runs the exact pairing crash matrix before reservation, at prepared, after durable
dispatching before peer call, after peer effect before response, after response before local commit and after
commit before client reply. Same operation/fingerprint returns one endpoint/grant; changed fingerprint
conflicts; reconcile performs peer lookup only. It exercises every endpoint transition, retire cascade/delete/
late-ack fence and regrant, then independently saturates 64 endpoints, grant 32-per-endpoint and 2,048-global,
10,000/64-MiB control, 10,000/16-MiB outbox, 16-KiB item and 100,000/256-MiB terminal audit boundaries.
Each N+1 has zero peer/gateway/Attention effect. Exactly the eighth gateway submission is permitted, the ninth
is absent,≤15-minute backoff is enforced, and seven/180-day folding retains the declared delivery/control/id/
generation/scope/correlation/replay fences. Prepared restart/expiry at exactly 600 seconds atomically
tombstones the reserved endpoint and expires the initial grant with zero peer effect; refused/not-paired
tombstones it and invalidates the grant. An awaiting-peer deadline instead enters reconcile-required. Each
case releases live endpoint/grant capacity without a second user action and late peer evidence can only
reconcile the retained correlation, never resurrect ids.

## Authority, collaboration, scale and quality

| Acceptance | Requirement | Evidence | Passing oracle |
| --- | --- | --- | --- |
| `ACP-SAF-001` | `PRD-SAF-001` | A, D | Missing/wrong auth, scope, operation id, revision and generation fail before side effects; exact retries return one receipt. |
| `ACP-SAF-002` | `PRD-SAF-002` | A, D | Cross-use, theft after expiry, escalation and revocation cases cannot exchange administrative/control/context/remote/companion capabilities. |
| `ACP-SAF-003` | `PRD-SAF-003` | A, D | Importing a hostile shared workspace creates no process, consent, credential/account binding or authority until an explicit local adoption receipt. |
| `ACP-SAF-004` | `PRD-SAF-004` | A, D | Disconnected/reordered clients recover from authoritative snapshot+journal; edges/scalars/immutable definitions/append-only runs/lifecycle ids follow the §14 conflict rules and deletion fences prevent resurrection. |
| `ACP-SAF-005` | `PRD-SAF-005` | A, D, P | Two simultaneous viewers cannot interleave input; 15-second lease expiry/renewal and atomic visible transfer are generation-fenced, and each unsent draft remains only on its original client. |
| `ACP-SAF-006` | `PRD-SAF-006` | A, D | Crash at every external-effect boundary yields exactly one definite/uncertain/manual state and no automatic duplicate launch/write/message/cleanup. |
| `ACP-SAF-007` | `PRD-SAF-007` | A, D, L | Forced provider/model/flag/host/context/telemetry failures show exact effective behavior or refuse; forbidden local/fresh/fabricated fallback never occurs. |
| `ACP-SAF-008` | `PRD-SAF-008` | A, D, P | Closed-inventory guard rejects unknown durable data; report/export/delete/retention exercises every closed PrivacyScope tag—installation, Workspace, Session, agent, Note, WorkItem, WorkItemSource, Resource, NativeJob, FlowRun, AccountProfile and RemoteClient—and remote tombstones reach proved/pending states. Seeded RuntimeAttachmentReceipt, RuntimeViewReplayFence and ContainerCloseReceipt rows appear exactly once under their declared owner/redacted metadata category; scoped delete removes only eligible richness while retaining mandatory operation/fingerprint/attempt or container-tombstone replay fences, and compact/export never emits PTY, endpoint-proof, process command/environment or survivor bodies. Their count, item, aggregate-byte, 180-day and permanent-fence boundaries are independently mutated. Per-scope stale owner/content/graph/Attention/authority and concurrent link/read/demand/mutation/binding fixtures refuse atomically; accepted deletes retain every required replay/route fence and report all survivors/residuals. |
| `ACP-SAF-009` | `PRD-SAF-009` | A, D, L, P | Invitation creation crashes before/after verifier/activation prove prepared never leaks/activates and the 600-second TTL. Exactly redeem and open are admitted as rate/fingerprint/replay-fenced bootstrap frames; every other pre-session name fails. Concurrent redeem, lost bearer response and same-id device-signed credential rotation preserve preassigned ClientId/SessionId and one receipt. Connecting/negotiating success, failure, 60-second timeout, daemon restart, active disconnect, reconnect, 24-hour session expiry and explicit session revoke cross the exact atomic Client/Session reducers/cascades and one-live-session rule: every negotiating/active child-session terminal path not caused by a terminal-client cascade disconnects a reusable connecting/active client and a fresh open succeeds once, while client revoke/expiry remains terminal and prevents open. Origin/key/role/scope/manifest and full-GUI/headless/Companion matrices reject escapes. Presence/selected ViewTarget expires in 30 seconds, stays out of durable sinks and grants no authority. |
| `ACP-SAF-010` | `PRD-SAF-010` | A, D | Snapshot R/event R+1, ack, gap, compaction, generation, offline draft and per-object conflict fixtures converge; cursor loss forces resnapshot, stale mutations refuse and compacted deletion ids never resurrect. |
| `ACP-SAF-011` | `PRD-SAF-011` | A, D | Imports with colliding/hostile ids remint every semantic object through one package map, omit all forbidden runtime/authority ids, preserve inert origin hash and cannot mutate or resurrect pre-existing state. |
| `ACP-SAF-012` | `PRD-SAF-012` | A, D, P | A bijection fixture enumerates exactly ten Companion tags, with `launch_allowlisted_agent` as the sole tenth mapping and accepts it only under the live exact local-reviewed grant and allowlist proved by `ACP-CRE-011`. Before every canonical effect, ActionId/operation/mapping/request-hash/principal/session/revisions/≤30-second expiry and receipt capacity are durable. Crashes at prepared/dispatching/canonical-effect/receipt boundaries reconcile the canonical receipt without redispatch; same-id replay returns it and changed payload conflicts. N+1 active/terminal capacity refuses before effect. Direct aliases, unknown tags, expired/offline/old-session actions and role escapes fail. Free text is non-sensitive only, ACP-ATT-011 is the sole permission path, forbidden authority/destructive/integration shapes remain absent and writer handoff is visible before bytes. |
| `ACP-SAF-013` | `PRD-SAF-013` | A, D, P | Boundary clocks/counts enforce each §14 retention value, referenced Flow revisions survive, ephemeral presence/drafts are absent and export/delete accounts for every retained record. Journal fixtures independently fill Installation=512 MiB, one Workspace=256 MiB, one ExecutionTarget=128 MiB and aggregate=4,096 MiB; local N+1 reserves/compacts or refuses before effect, external overflow leaves current-state+gap, and a slow client must resnapshot. Crash at every multi-stream barrier fragment yields all-or-none vector visibility. Thirty-day/byte compaction retains current/nonterminal/uncertain intent, operation replay, deletion/nonreuse and unresolved barrier fences. |
| `ACP-SAF-014` | `PRD-SAF-014` | A, D | The verifier accepts exactly the frozen ledger schema/snapshot/count and one unique row per stable feature id with allowed disposition, nonempty rationale, 64-hex evidence and existing PRD/ACP/ADR links. Given a bare or worktree repository containing the opaque frozen snapshot, the source verifier recomputes the complete tree digest, all 112 evidence-reference digests covering 89 unique locator/digest pairs and the selector-v1 census of 630 production/product-document modules plus 756 closed IPC, NodeKind, built-in-adapter, adapter-capability, settings-section, command/family, shortcut, hideable-control, union and safety-constant surfaces: 1,386 candidates total. A separate 2,188-row dense normalized mapping permits each candidate to map to one or more capabilities, requires every candidate and every CAP to be covered, preserves exact disposition and source/key anchors, makes every ledger-evidence relation primary and permits `supporting` only for audited modules that explain why they add no independent capability. Worktree/bare fixtures and mutations prove exact failures for ambient Git overrides, snapshot/tree/blob mismatch, omitted/new/duplicate candidate, unknown candidate/CAP, missing many-to-many evidence relation, broken disposition/basis/resolution/rationale/anchor, changed adapter-capability membership, deleted runtime styling, moved hideable controls, changed PTY safety constants and deleted Settings keys; coordinated authority rewrites still fail before specification completion. |
| `ACP-SAF-015` | `PRD-SAF-015` | A, D, P | A bijection enumerates exactly `set_tree_expanded`, `set_tree_expanded_all`, `set_tree_presentation`, `select_tree_node`, `set_surface_view_mode`, `set_inspector_width`, `set_board_presentation` and `set_terminal_appearance` as wire requests and history variants. Each records owner/surface/session, operation id, exact pre/post revisions and inverse; the expand-all inverse records the prior default plus bounded exception set and never enumerates hierarchy rows. Undo/redo CASes the current history/object generations and replays one receipt. Two clients and two surfaces prove cross-owner undo is refused; new edit after undo, session disconnect, target deletion, compaction and restart deterministically preserve, invalidate or clear only the declared owner stack. Attempts to include runtime/input/provider/source/SCM/Attention/context/grant/credential/destructive effects are structurally refused, and undo/redo emits none of them. |
| `ACP-SAF-016` | `PRD-SAF-016` | A, D | The frozen source census maps every commercial licence/upgrade/seat/entitlement channel and settings surface to one `irrelevant` capability row with an explicit rationale. Generated product registries, settings schemas, protocol request/event unions, role matrices, telemetry and launch receipts contain no monetisation or entitlement gate; fixtures with absent, invalid or expired commercial state leave every product capability unchanged and emit no network request, identity collection, Attention or downgrade. Reclassifying any such candidate as adopted/adapted, deleting it from the census or adding a product gate fails before specification completion. |
| `ACP-SAF-017` | `PRD-SAF-017` | A, D, L, P | Two peers exchange bounded UTF-8 chat only inside the exact shared Workspace/ViewTarget and current sender/session revisions; wrong scope/revision, duplicate, stale, over-rate, over-size, control/bidi/credential-bearing and disconnected-sender payloads are refused or redacted without cross-project leakage. Expiry, explicit retract, peer departure, view change, disconnect and crash/reconnect remove the projection and cannot resurrect it. Store/protocol/packet inspection proves chat creates no durable message, context packet, terminal byte, command, permission answer, Attention, focus change, notification, provider call or control grant; presence loss never blocks ordinary terminal or agent work. |
| `ACP-SAF-018` | `PRD-SAF-018` | A, D, L | Generated network/client/settings/store/protocol registries and executable/source scans fail on any product telemetry endpoint, analytics or install-count payload, stable telemetry id, event queue, opt-in setting or always-on send path. Instrumented startup, runtime, failure, update, collaboration and shutdown fixtures emit zero analytics requests and persist zero analytics identity/event. A separate signed-update fixture may make only its declared purpose-bound metadata request with no installation/user/workspace id or usage event; changing its endpoint/schema/storage/receipt into a general event channel fails. The frozen rejected CAP and every telemetry/check/privacy source candidate remain explicitly mapped, so deletion, relabelling, adoption or concealment as generic support fails the source gate. |
| `ACP-SAF-019` | `PRD-SAF-019` | A, D, L, P | Invalid JSON/schema, truncation, hostile bytes, concurrent writer and permission/disk-full fixtures prove the exact unreadable source bytes are fsynced into a non-colliding hash-addressed quarantine descriptor before any default/empty publication or later save. Crashes before/after read, quarantine write, rename, descriptor commit and empty-state publication reconcile one idempotent outcome: original intact or one verified quarantine, never overwrite, partial success or evidence loss. Count/byte bounds refuse a new load before destructive effect and expose capacity recovery. One scoped status/banner names honest failure and offers exact inspect/export/reviewed import; dismissal changes no bytes, import cannot gain runtime/authority, and retry/restart emits neither duplicate quarantine nor silent success/data deletion. |
| `ACP-SAF-020` | `PRD-SAF-020` | A, D, L, P | Two authenticated clients concurrently add, edit and delete disjoint and same fields through one daemon StateStream. Exact operation/client/object revisions deduplicate self-echo, additive discoveries appear once, dirty local presentation yields a fieldwise conflict/merge instead of overwrite, and authoritative lifecycle/runtime evidence is never replaced by stale presentation. Disconnect, cursor gap, daemon restart and resnapshot converge with one canonical revision and no lost Session/runtime. Watcher delete/recreate/rearm races and hostile direct filesystem modifications of the internal store create no parallel authority: bytes are refused/quarantined with an exact status and cannot publish empty state. Only an independently reviewed bounded PortableImport may introduce external package data; file-copy/watch events cannot bypass reminting, validation or authority stripping. |
| `ACP-SAF-021` | `PRD-SAF-021` | A, D, L, P | Age, detached-count, memory/swap/PSI and PTY pressure are swept independently at every threshold and race. A kill/reap oracle records zero tmux-session/process termination, zero End/Delete and zero lifecycle dispatch for every pressure/cap-only input, including maximum pressure and all-detached inventories; only proved reconstructible viewer/renderer/PTY-client trims and canonical Status/Attention may occur. Unknown ownership, watcher, plain-shell, current input, working/prompt/Attention and stale handle/generation each protect work. Eco remains default-off and passes only its separate exact eligibility proof. An individual-runtime foreground terminate/kill fixture pins exact target, handle, process/session start identity and generation with durable receipt; cancel, stale, unknown and crash reconciliation never replay or broaden it. Separate Session End fixtures prove `keep_processes` sends zero stop and a terminate-or-kill disposition with one stale plus one unknown child still removes the exact reviewed container in one action, inventories both uncertain children from the daemon-derived current vector and never weakens into an individual wildcard signal. Mutation tests that restore source-style `planReap` output from pressure/count or make stale child evidence veto End fail this oracle. |
| `ACP-SCL-001` | `PRD-SCL-001` | A, M, P | The fixed minimum profile runs 50 Sessions, 100 live runtimes and 1,000 nodes with concurrent Attention for the declared duration while all correctness and latency budgets pass. |
| `ACP-SCL-002` | `PRD-SCL-002` | A, D, M | Viewport materialisation and large-payload subscriptions remain bounded; queue saturation recovers by revision and terminal input is never blocked. Terminal fixtures independently fill attachments 64/Surface,256/connection,4,096/32 MiB; 2-MiB baselines/256 MiB; output 512 chunks-or-8 MiB/PTY and4,096/256 MiB; 128 one-MiB pump batches; and outboxes 256 frames-or-8 MiB/connection plus4,096/128 MiB. Buffer-first overflow gaps only the producing terminal and never evicts a sibling. Every cells/bytes gap delivers through the non-borrowable critical partition, retires exactly the old attachment/baseline/batch generation and triggers client-runtime `resync_pane` with zero operator action; omitted, stale/wrong-stream or wrong-owner generations, a still-active identity and replay of an already-replaced identity create no state. The 65,536-cell replacement is chunked when needed and installed atomically under fresh identities. The non-borrowable32-frame/1-MiB connection and512-frame/16-MiB global critical partitions keep input receipts, Attention, control and gaps within latency under terminal flood. Chunked responses test RequestId 128/129 bytes, raw chunks 180 KiB, four streams/connection,16/120 MiB global, contiguous offsets/digest, dropped/duplicate chunks and disconnect; no frame exceeds256 KiB and no partial screen/hierarchy/image applies. Independent hierarchy fixtures hit4,096 small operations and180 KiB, with the first excess producing one scoped gap+automatic atomic refresh and no fragmented push. Image fetch reaches eight/Surface,32/connection,128 count and4-MiB item/128-MiB family, transfers without a second shared charge, automatically retries at most once and otherwise leaves a labelled placeholder. Every detach/reselection/resize/gap/Attempt/PTY/buffer/connection/process edge proves exact attachment/baseline/batch/fetch/outbox release. |
| `ACP-SCL-003` | `PRD-SCL-003` | A, D, M | Every §13 pressure source degrades/refuses and recovers as declared, preserves every runtime absent an external OS kill and records that Turn issued no undeclared signal; proposed intervention appears as exact Attention. |
| `ACP-SCL-004` | `PRD-SCL-004` | A, P | The detailed accessibility matrix reaches every named kind/surface/action at min/max zoom, reduced motion and active IME with names/roles/order/focus/live-region oracles and non-colour status. A terminal-appearance fixture changes font family/size, theme, foreground/background contrast, line spacing and zoom through keyboard and screen-reader-labelled controls; the live non-interactive TerminalPreview updates each exact value, meets contrast/clip/reflow bounds, sends zero PTY/control bytes and matches a real terminal rendering snapshot under the same resolved settings. |
| `ACP-SCL-005` | `PRD-SCL-005` | A, M | Artifacts from the fixed profile report raw workload and p50/p95/p99 route/view/input plus numeric memory/disk/queue values; every published bound independently fails on regression. |
| `ACP-SCL-006` | `PRD-SCL-006` | A, D, L | The named duplicate/stale/outliving/offline/conflict/writer/reboot/remote matrix converges without false counts, resurrection, lost Attention or duplicate effects. |
| `ACP-SCL-007` | `PRD-SCL-007` | A, D, M, P | The 30-minute sustained+burst run on the minimum profile stays within every numeric budget while individually saturating GPU/memory/PTY/fd/process/disk/journal/hook/remote/collector boundaries and retaining exact recovery evidence. RuntimeAttempt and terminal-state counts are varied independently: 10,000 nonterminal attempts may be non-PTY, while exactly 128 resident PTYs reserve 256 MiB raw rings plus 512 MiB maximum parsed-screen family state and all terminal image/client-cache growth contends in the same 1,024-MiB shared pool without exceeding the 1.5-GiB Turn total. A generated bijection admits only the nine AuxiliaryWorkerOwnerKey tags and independently fills their per-kind counts 1/32/128/128/32/1/32/32/64, the cross-kind128 count with small workers, the≤128-MiB item limit and exactly1,024 MiB with eight maximum workers. Every boundary/N+1 proves atomic kind+family+shared+cleanup reservation before process/task/socket/source/network effect. Unknown/relabelled kinds, a 129th cross-kind worker and the ninth maximum worker park/refuse; cancellation, two-second shutdown, hung tree, owner/generation loss and repeated owner recreation retain the exact family slot/bytes in ProcessCleanupCharge until quiescence or OS-reclamation proof. Speech, Browser, Media and proposal helpers remain in their stricter families. |
| `ACP-SCL-008` | `PRD-SCL-008` | A, P | Packaged platform/screen-reader runs execute every row added to `ACCESSIBILITY_ACCEPTANCE.md`, including non-drag alternatives, deterministic focus restoration and companion/remote-handoff semantics. |
| `ACP-SCL-009` | `PRD-SCL-009` | A, D, L, M, P | Desktop, authenticated full-remote GUI and headless clients consume identical objects/snapshots/events/routes under latency/backpressure bounds; reconnect/gap/writer handoff converges, while CSRF/replay/origin/storage attacks and every desktop-only mutation fail server-side with local revocation/audit proof. A generated exact bijection compares the normative registry, handshake manifest and closed role tables. `full_gui` must include scoped `create_agent_instance`, `create_runtime_node`, `create_resource_node`, `create_group`, `create_browser_node`, `switch_agent_configuration`, `set_group_membership`, `move_group_subtree`, WebPreview load/get/reconcile/close, Browser navigate/back/forward/reload/stop/storage-clear/reviewed-popup/reviewed-download plus scoped get/reconcile recovery, `create_work_item`, every declared surface/tree/Attention navigation request and acquire/renew/handoff/release/write/resize input lease request. `headless_status` contains only pure-read/subscription/cursor/navigation classes, so it may get/close only its scoped WebPreview load state and cannot load/reconcile; Companion contains only its ten mappings—including grant-gated `launch_allowlisted_agent`—and named read/presence/grant-ack exceptions. Deleting, renaming, reclassifying or role-removing each required name fails the manifest mutation suite, so negotiating an empty ordinary set cannot pass. The full-GUI role matrix proves exact MediaImport/Transfer saga cancel is allowed only for the scoped revision and has zero publication, while forged container/provider/job/runtime cancel/abort/stop/terminate remains denied. Packaged screen-reader/zoom evidence applies to desktop/remote GUI; headless instead passes deterministic structured schema/order/error oracles and makes no rendering claim. |
| `ACP-SCL-010` | `PRD-SCL-010` | A, D, L, M, P | Desktop and companion show the same per-provider/profile/target context windows, quota windows/resets and cursor-bounded conversation/job/Attention activity references; partial/empty/stale/rate-limited/error fixtures never render false zero/empty authority, caches never cross profiles and projection dismiss/read cannot resolve provider work. |
| `ACP-SCL-011` | `PRD-SCL-011` | A, D, L, P | Diagnostics render only the bounded redacted structured status/log family under its declared retention cap and label exact coverage, gaps and freshness. Filter and copy are derived from the same canonical rows; canary credentials, environment values, raw terminal/transcript bodies and unauthorised absolute paths are absent from view, clipboard, logs and crash data. Clear is explicit, idempotent and scoped, while restart/reconnect cannot resurrect cleared payloads beyond required deletion fences. Full/overflow/gap, filter/copy, clear race, offline and cancellation fixtures preserve terminal input and Attention and never fabricate completeness. |
| `ACP-SCL-012` | `PRD-SCL-012` | A, D, P | A generated registry enumerates every stable settings section exactly once and proves each is reachable by sidebar, search, keyboard and exact deep link with deterministic focus. Search returns the same canonical row rather than a duplicate editor. Every row names effective value plus Global/Workspace/Template/Session/Temporary source; stale scope/revision edits refuse with current truth and exact precedence. Per-section reset previews the exact changed keys, preserves every other section and requires no extra confirmation when consequence-free; cancel changes nothing, apply is one revision-fenced receipt, retry is idempotent and concurrent edits conflict rather than overwrite. Unknown/deleted/duplicated section ids, broken deep links, hidden searchable rows and a reset that touches an unrelated key fail the generated/mutation oracle. |
| `ACP-SCL-013` | `PRD-SCL-013` | A, D, L, P | A bug-report action creates one local editable draft from a pinned diagnostic revision with an exact inclusion manifest and redaction report; it neither opens an external issue nor performs network/export before a separate foreground review action. Canary credentials, environment values, raw terminal/transcript bodies and unauthorised absolute paths are absent from the draft, link query, clipboard and exported report. Edit, cancel, offline, stale-source and external-link refusal fixtures leave diagnostics, terminal input and Attention unchanged; retry of the reviewed export/link intent is idempotent and cannot silently broaden the included data. |

Remote recovery is part of the cited rows, not an optional transport test. `ACP-RUN-006` and `ACP-RUN-010`
each lose a full-GUI remote reply, reconnect, then get/reconcile only the original scoped
RepositoryMutationIntent or FileSaveIntent; sibling owner/scope access is refused and no Git/network/write/
replace effect repeats. `ACP-RUN-011` does the same for WebPreviewLoadIntent, BrowserNodeCreationIntent and
BrowserDownloadQuarantine, proving no repeated fetch, second page load or body transfer. `ACP-SCL-009` mutates the generated
registry/handshake/role manifests and fails if any of `get_file_save`, `reconcile_file_save`,
`get_repository_mutation`, `reconcile_repository_mutation`, `get_browser_node_creation`,
`reconcile_browser_node_creation`, `get_browser_navigation`, `reconcile_browser_navigation`,
`load_web_preview`, `get_web_preview_load`, `reconcile_web_preview_load`, `close_web_preview`,
`clear_browser_storage`, `get_browser_download_quarantine`,
`discard_browser_download_quarantine` or `reconcile_browser_download_quarantine` is absent, misclassified or
accepted outside exact creator/invitation recovery and object scope. The six `get_*` requests are pure reads;
reconcile/discard operations are lookup-only domain mutations and never widen the underlying authority; storage
clear is a separately consequence-reviewed scoped domain mutation.

`ACP-CRE-008` independently runs RepositoryPublishIntent through prepared, creating-remote, remote-created,
pushing, remote-published, configuring-upstream, every phase-specific reconcile-required state and all terminal
states. Crashes before dispatch, after external effect, after receipt and before reply prove that get/reconcile
uses only exact provider correlation, remote old/new object ids and local config identity; it never creates,
pushes, configures, rotates credentials or reacquires a lease. Same-id replay advances only from a proved phase.
Every state-table cell asserts monotonic highest_applied_phase. `no_effect` is accepted only from creating-remote
with complete proof of zero remote/config effect; a proved created repository plus no push terminalises partial
at remote-created, and a proved pushed ref plus no config terminalises partial at remote-published. No failure,
reconcile or compaction may erase/downgrade that prefix, and only prepared may cancel.
The suite independently fills256 nonterminal intents,10,000 small records or8,192 maximum≤8-KiB records/
64 MiB, plus100,000 small replay fences or98,304 maximum≤512-byte fences/48 MiB. It races the
one-per-repository/destination key and mutates count/bytes/fence/terminal/
journal/correlation/recovery/180-day bounds independently; every N+1 has zero
host/Git/config/filesystem/lease effect and primary `main` stays switchable.

For `ACP-LIF-006` and `ACP-VIE-008`, “every saga” also includes RepositoryPublishIntent. Prepared publication
cancels only with no-effect proof; remote-created/remote-published/configuring/reconcile-required evidence enters
the pre-reserved recovery inventory with exact destination/object/ref/config/correlation identity while the
ended lease is revoked. Cleanup never deletes provider data, resumes a later phase, blocks row removal or
touches primary `main`.

`ACP-VIE-012` additionally admits exactly four interactive WorkItem source queries/connection and32 global,
2 MiB/item/64 MiB family plus shared RSS before provider read. Independent fixtures hit500 small summaries,
the1-MiB page boundary,2-KiB item and512-byte cursor; row501 or the next byte yields an exact partial cursor,
while a513-byte/stale/wrong-generation cursor, oversize provider page or 33rd query gaps/refuses without a
provider follow-up or false complete. Completion, failure, cancel,30-second deadline and disconnect release
the exact WorkItemSourceQueryBuffer after I/O quiescence. Body/comments/credentials/raw provider fields never
enter safe summaries; atomic response completion/failure leaves zero retained WorkItemPage bytes.

`ACP-RUN-006` also admits exactly 32 DirectoryWatches per connection and 2,048 installation-wide from complete
scans, refuses each N+1 before backend subscription, then independently releases on explicit unwatch,
overflow/cursor-loss/source-or-target invalidation/generation-change terminal gap and connection loss. Every
gap requires resnapshot and reconnect cannot inherit an id. `ACP-SCL-009` requires both
`close_directory_scan` and `close_command_catalogue_scan` in `cursor_mutation` for scoped full-GUI/headless
clients; removing/reclassifying either fails the registry/handshake/role mutation suite, and Companion remains
unable to name them.

The same `ACP-RUN-006` fixture independently saturates DirectoryScan at16/client,1,024 count,16-KiB item and
16-MiB family; DirectoryWatch at32/client,2,048 count,8-KiB item and16-MiB family; CommitGraph request values at
500 nodes/2-KiB node/1-MiB page/10,000 traversal; and changed-file request values at1,000 rows/2-KiB row/2-MiB
page with a512/513-byte cursor boundary. The 2,001st directory entry continues rather than truncating, and a
4-MiB page uses atomic chunked transfer. Count/byte N+1 reserves before backend work; final page, explicit
close, invalidation, disconnect and60-second TTL release exact scan metadata, while every request-only page has
zero retained state after response completion/failure. Cycles, missing objects, oversize row and stale cursor
produce typed gaps and zero repository/file effect.

`ACP-CRE-009` independently fills CatalogueScan at8/client,512 count,32-KiB item and16-MiB family, then proves
final page/close/context invalidation/disconnect/30-second TTL release and zero retained CommandSearchResult
bytes. `ACP-RUN-004` fills TextSearchSession at8/Surface,512 count,16-KiB item and8-MiB family; pages are
request-only≤200 matches/1-KiB match/200 KiB, while a scan observes exactly10,000 without retaining10,000
objects. Count/item/family/10,001st-match and15-minute TTL boundaries are independent; stale source releases
metadata and later movement returns stale rather than false no-match or terminal input.

`ACP-CTX-013` independently fills ConversationInventory query buffers at4/connection,32 count,2 MiB/item and
64 MiB family; result fixtures hit500 small descriptors,1 MiB,2-KiB item,10,000 scanned candidates and a
512-byte authenticated source cursor. Row501/next byte is partial; raw-provider oversize,candidate10,001,
cursor513/stale/generation mismatch,rate limit and incomplete cache never prove complete/zero. Completion,
failure,cancel,30-second deadline and disconnect release after I/O quiescence; cross-profile canaries and raw
pages remain absent and request completion retains zero ConversationInventoryPage bytes.
Conversation adoption then independently fills100,000 small or65,536 maximum4-KiB terminal receipts under
256 MiB; each count/byte N+1, concurrent global-owner CAS and crash boundary leaves either one stopped
preassigned Node/AgentInstance/binding with its original receipt or no identity/effect, never a partial row or
provider/launch/input call. Rich detail compacts only after180 days and an installation-lifetime fingerprint/
ConversationKey/preassigned-id/result fence exists.

`ACP-ADP-011` independently admits8 NativeJobScans/connection and512 count at32 KiB/item/16 MiB, plus4 page
buffers/connection and32 count at2 MiB/item/64 MiB. It hits500 small jobs,1 MiB,2-KiB item,10,000 observed
jobs and a512-byte chained cursor. Every count/item/family/row501/next-byte/candidate10,001/cursor513 N+1,
oversize raw page,stale generation and rate-limit path gaps/refuses before a new observation and never evicts a
live scan or claims absence. Final page,gap,generation loss,disconnect,60-second TTL and30-second page deadline
release exact scan/page state; reconnect inherits none and request completion retains zero NativeJobPage bytes.

`ACP-VIE-014` independently pages200 small events and exactly128 maximum8-KiB events into the1-MiB boundary,
then rejects/gaps byte N+1 and a513-byte/stale checkpoint cursor. Count and bytes are not asserted
simultaneously. Each request-only page is complete/partial/gapped, transfers atomically and leaves zero retained
page state; compaction preserves the checkpoint and never changes the current WorkItem.

`ACP-SCL-002` fills each of the seven LiveSubscriptionRegistry families and then the shared 64/connection,
4,096-installation, 4-KiB-item/16-MiB-metadata boundaries. It independently saturates 64 events/1 MiB per
subscription, 256 events/8 MiB per connection, 4,096 events/64 MiB installation-wide and the shared variable-
RSS pool. Every N+1 registers no producer and retains existing ids; identical key returns one id, changed bounds
swap only after reservation, and forged sibling unsubscribe changes nothing. Overflow emits the reserved
terminal gap and releases; unsubscribe, scope/source/owner/Surface/session/connection/process loss release, and
reconnect inherits none. DirectoryWatch retains its separate count tests while charging these queue pools.
For every closed subscription/ContentKind it independently emits an exact180-KiB serialized event and a
one-byte/one-operation excess: no complete frame exceeds256 KiB, excess consumes the reserved gap, and the
client runtime performs the exact automatically chunked snapshot/read with atomic apply and zero operator
action. Dropped/reordered/stale response chunks render no partial body. A1-MiB queue therefore never licenses
a1-MiB unsolicited event or fragmented push.
Push schema tests require exact `resource_inventory_changed`, `target_recovery_changed`,
`account_activity_changed` and `live_notification_status_changed` identities. `ACP-SCL-009` requires scoped
runtime/resource reads plus resource/live-notification subscriptions for eligible full-GUI/headless roles,
retains Companion's state/account-only exception and proves target-recovery read/subscription/push is absent
from every remote registry/manifest while remaining bounded for a local administrative surface.

`ACP-SCL-009` generates the complete family manifest from every protocol/schema/settings `state_family`
annotation, compares name, lifetime and exact owner-key expression to `StateFamilyManifest.vNext` in
`docs/PROTOCOL.md` and proves a bijection. Installation, Workspace and ExecutionTarget durable ownership is
exact; every ephemeral family must match its complete connection/Surface/subject/generation key rather than a
lifetime-only bucket. Removing SurfaceRegistry/Surface/TreeSurfaceState/high-water/SurfaceHistoryIndex,
Template/settings, RemotePresence, any authentication/notification/checkout/browser/file/repository/model/
conversation intent, WebPreviewLoadIntent, FileEditSnapshot, BrowserLocalSnapshot, ContextPacket/AgentMessage/
voice ephemeral state or adding an unclassified, aliased or default-owned schema family fails. Marking a
state-bearing object as request-only, changing a canonical family name, omitting one owner-key component or
relying on nesting/wildcards also fails. Every other
document is checked to reference that inventory rather than maintain a divergent closed list.

`ACP-CRE-003`, `ACP-RUN-007` and `ACP-SCL-007` independently fill 1,024 Workspaces; 1,024 Sessions/Workspace and
10,000 installation-wide; 10,000 base Nodes/Session, 50,000/Workspace and 100,000 installation-wide; 50,000
AgentInstances; 10,000 live and 100,000 current+ended RuntimeAttempt details; and 64 Panes/Session plus 4,096
installation-wide. Empty Workspace/Session, every Node kind including Group and WorkItem shell, Pane and Layout
are charged. A mixed fixture then fills the shared 200,000 semantic-core records and 1,024 MiB using≤64-KiB
ordinary records and≤256-KiB Layouts, including TeamMembership/DependencyEdge/Flow core. Each family-count,
shared-count, item-byte and aggregate-byte N+1 names the exact saturated key and produces zero id, graph,
filesystem/worktree, PTY/process, provider, file, renderer or network effect; unpausable external discovery emits
one bounded coverage gap and cannot claim absence/control. Eligible ended Attempt details fold first, while a
current/live/referenced record never evicts. Delete/tombstone frees capacity only after the nonreuse/reference
fence commits, and End succeeds at full capacity from existing recovery reservations. Separately, 256 terminal
Panes reserve the full 8-MiB journal+4-MiB checkpoint allowance in exact 2,048/1,024-MiB physical pools before
launch; the 257th history-enabled Pane launches nothing, and rotation/replacement never double-charges.

`ACP-SCL-007` obtains `PhysicalDiskLedger` reports and independently fills all ten closed classes at
8/4/3/2/2/2/8/100/4/2 GiB, including 10-GiB/Workspace Media sublimits, then fills their exact 135-GiB total in
a quota-backed deterministic backend without requiring a 135-GiB CI write. Each family-byte and total-byte N+1
refuses before file/chunk/effect. Real-file fixtures cover allocate/extend, SQLite/WAL sidecars, sparse,
compression, COW/reflink, hardlink/refcount, copy, rename, seal, crash, cleanup and boot reconciliation; every
physical extent is charged once, a copy charges both, ownership transfer never double-counts and unknown
Turn-root bytes enter `operational_store(unclassified_quarantine)`, charge its 8-GiB cap+the total, raise
Attention and block new writes until classified/removed without becoming an eleventh class. The report exposes reserved logical, allocated
physical and reclaim-pending bytes per class plus total. Removing/reclassifying FileSave/portable/account-root/
speech/sync/terminal payloads as “operational exclusions”, hiding scratch in an external checkout/provider root
or omitting any Turn-owned path fails. End/delete still succeeds at all caps from pre-reserved cleanup metadata.

`ACP-HIE-010` and `ACP-SCL-009` prove SurfaceId is daemon-minted/non-reused and owned by the authenticated local
identity or RemoteClient, never caller-created. New/resume/replacement-connection races admit four live Surfaces
per connection, eight live+dormant per owner and 64 installation-wide within 256 KiB/item and 16 MiB aggregate;
each N+1, changed-owner resume and retired/expired id changes no state or temporary Pane. One Surface uses
one exact≤4-KiB SurfaceConnectionBinding; the suite independently fills4/connection and64/256 KiB globally,
then proves fifth/65th/item-byte N+1 mints/transfers nothing. Resume reserves before atomic swap, predecessor
disconnect/replacement releases authority+bytes before dormancy, and reconnect cannot inherit its generation.
One Surface uses
`expansion_default=collapsed|expanded` plus 2,000 unique exception keys, 2,000 manual-order keys and 32≤256-byte
filters; expand-all at 100,000 Nodes toggles the constant-size default and clears exceptions, a single opposite
toggle creates one exception, and exception 2,001 plus every other item/encoded-byte N+1 refuses atomically.
Disconnect revokes every ephemeral child's authority and releases every quiescent child; a deliberately hung
renderer/decoder/fetch worker atomically transfers its original family/shared reservation to a pre-reserved
ProcessCleanupCharge, leaves only body-free cleanup metadata plus the 30-day dormant record and cannot veto
disconnect/retire/End. Exact resume
restores it, while retire/owner deletion/expiry removes state+PresentationHistory after nonreuse high-water.
`get_hierarchy` performs no claim/mint/transfer. The registry mutation suite requires `open_surface` and
`retire_surface` as cursor mutations for full-GUI/headless and the exact Companion exception; forged sibling
retire, reconnect cloning and omission/reclassification fail. Schema mutation accepts only
`HierarchyKey=workspace|session|node` inside TreeSurfaceState; replacing `node` with legacy `process` or omitting
any closed NodeKind fails round-trip/selection/expansion for Agent, Group, Note, File, Diff, WebPreview, Browser,
Media, WorkItem and Job fixtures.

The same Surface suite fills eight TemporaryPanes/Surface, 32/connection and 512 installation-wide at≤4 KiB
each/2 MiB aggregate, then proves every count/byte N+1 mints nothing and launches no process/PTY/renderer.
Close, source invalidation, exact 30-minute expiry, disconnect, retire and daemon restart release; promotion
first saturates durable Pane/Layout/core capacity to prove failure preserves the temporary view, then succeeds
as one atomic ownership swap with no duplicate Pane.

The same local-client fixture fills TemporarySettingsRecord at256 keys/Surface,4 KiB/value,64 KiB/record,
8/client and64/4 MiB globally; LocalInputDraft at1/Surface,8/client,64 count,32 KiB/item and2 MiB; and
ImeComposition at1/Surface,8/client,64 count,32 KiB/item and2 MiB. Count/item/aggregate N+1 are independent and
change no target/settings/draft. Same-window daemon reconnect preserves validated temporary settings and draft
text but every send requires a newly reviewed exact InputTarget; it cancels active IME and sends zero composed
bytes. Focus/target/Surface/window/client loss cancels IME, Surface/window/client exit releases draft/settings,
and stale local ids never gain daemon authority. Typed and voice text share the one draft body rather than
double charging it.

`ACP-SAF-009` independently fills RemotePresence at128 count,4 KiB/item/512 KiB and RemoteReplayNonce at
10,000 small records or exactly8,192 maximum256-byte records/2 MiB. Every count/item/byte N+1 is reserved before
acceptance and emits zero remote mutation. Disconnect/exact30-second presence expiry emits one bounded
tombstone; receipt/session revoke/connection loss/exact10-minute nonce expiry releases once. Daemon restart
inherits neither family, stale replay cannot name a new session/generation, and no presence field grants
navigation,input,Attention or control authority.

Retirement, owner deletion and dormant expiry also fill the daemon-derived SurfaceHistoryIndex with 256
nonempty Workspace PresentationHistory partitions, race one mutation in every indexed stream and prove one
atomic barrier advances Installation registry/nonreuse high-water plus every exact Workspace history revision
before state release. The client cannot omit a partition or supply the revision vector. A 257th Workspace first
performs bounded eligible history compaction; if still full, navigation applies with
`history_not_recorded(capacity)`, grows no index and cannot prevent the same Surface from retiring.

`ACP-HIE-010` then fills 1,024 Workspaces, 10,000 Sessions and 100,000 Nodes. The vNext bootstrap index remains
≤6 MiB for all≤111,024 compact coordinates, the complete encoded result+NDJSON wrapper including first page
remains≤7,680 KiB as one logical result carried automatically in≤180-KiB raw/≤256-KiB encoded stream frames,
applies atomically after digest/generation verification, and neither contains label/body; visible summaries arrive only in
≤500-row/1-MiB pages with≤2-KiB rows. It fills 16 scans/connection, 1,024 global and 16-MiB scan metadata,
using no-caller-id begin for each installation/workspace/session/subtree scope and revision/ordinal/predecessor/
cursor-fenced continuation. It then proves each count/byte N+1 refuses without evicting a visible page and that completion/close/gap/scope/
disconnect/60-second expiry releases. The 16-KiB-raw/24-KiB-NDJSON filter bitmap produces the same ProjectedRows as a full oracle.
Viewport and overscan fetch automatically; keyboard selection, restore and Attention reveal an exact target
through≤128 Group ancestors+Workspace/Session and≤1 MiB without a Load-more action or label substitution.
Independent fixtures apply exactly4,096 small ordered delta operations and exactly180 KiB serialized once;
operation/byte N+1 emits one scoped gap in a complete≤256-KiB frame, refreshes the
compact index+affected visible pages and never sends a 100,000-row detailed replacement. Closed role/manifest
mutation requires get/page/reveal as pure reads and close-scan as cursor mutation for scoped full-GUI/headless,
with only the declared Companion bootstrap exception.

`ACP-RUN-010` mints daemon-only FileEditSnapshot ids and independently fills 16 per connection, 128 global,
8-MiB item and 1,024-MiB aggregate memory bounds; every N+1 refuses before descriptor read. Exact activity
refreshes 60 minutes; close, source/root/target invalidation, Surface/connection loss and expiry release, old
ids become stale and reconnect cannot inherit. External content change instead remains a conflict-capable base;
successful save advances that exact snapshot while conflict does not. `ACP-SCL-009` requires
`close_file_edit` as scoped cursor mutation for any remote role allowed to open, rejects sibling ownership and
fails registry/manifest mutation if close is absent or reclassified.

`ACP-RUN-016` independently fills one ContentProjection per Surface, four per connection and 64 installation-wide,
2-MiB item and 128-MiB aggregate bounds. Replacement reserves and sanitises before one atomic swap, so every
count/byte/invalid-content N+1 preserves the prior Surface projection and changes no source. Explicit clear,
source invalidation, Surface loss and connection loss release; reconnect inherits none. `ACP-SCL-009` requires
`clear_content_projection` in navigation for scoped full-GUI/headless roles, denies Companion and fails the
registry/manifest mutation suite if absent or reclassified.

`ACP-RUN-011` additionally restarts after quarantine reservation but before response body byte one and proves
the same `created_at+30 minutes` expiry releases count/shared-byte capacity with zero body/ticket/Node effect.
Its mutation suite independently changes local-snapshot count, aggregate MiB, item MiB, quarantine count,
quarantine item MiB, shared transfer MiB, TTL, terminal-receipt count and terminal-receipt days; each exact
boundary is authoritative, and no duplicate aggregate setting can disagree with the shared pool.

`ACP-SCL-007` and the cited RUN acceptance rows fill BrowserLocalSnapshot, FileEditSnapshot,
ContentProjection, Browser renderer/partition, Media decoder, SpeechWorker, subscription queues and helper
working sets first in isolation and then concurrently. Every reservation charges both its family cap and the
single 1,024-MiB variable pool while daemon/GUI/client core remains inside its non-borrowable 512-MiB budget.
The exact mixed N+1 refuses or parks before effect, terminal input/Attention budgets still pass, sampled total
RSS stays≤1.5 GiB, and mutation of either shared key or removal of any family charge fails the oracle.

## Critical end-to-end scenarios

The row matrix is exhaustive; these journeys catch integrations that can satisfy isolated component tests
while still failing the product.

### E2E-01 — mixed-provider delegated team

From a clean Workspace, one Flow action creates a conductor plus Claude Code, Codex, Gemini, OpenCode,
GitHub Copilot, Grok and a shell/log worker in separate writable worktrees. Children appear under the correct semantic parents even
when some have no PTY. Model/mode/account/context/quota fields show effective or honest unavailable values.
Independent results arm one synthesiser. Questions and the final review reach the exact views through
Attention. The primary `main` checkout remains available throughout.

### E2E-02 — nested children and truthful counts

Authenticated versioned runs require child and nested-child topology from all six dedicated adapters: Claude
Code creates exactly three children, Codex exactly five, and equivalent bounded counts are declared for
Gemini, OpenCode, GitHub Copilot and Grok. A dedicated adapter that cannot prove this topology fails the
dedicated-adapter acceptance contract; generic process inference is exercised only as additional degraded
evidence and is never a substitute. One child creates a
grandchild, one is discovered through process evidence before structured evidence, one finishes while its
parent remains busy and one outlives the parent. Direct/live/total counts and tree placement stay correct
across UI reconnect and daemon restart. A claiming adapter that fails to emit its observation degrades with
the exact mechanism/remedy; a partial adapter displays `N+ observed`; an unavailable capability displays
unsupported/unknown, never zero.

The same harness starts with a complete empty snapshot and proves `exact(0)`, then injects event 1,025,
drops a start delta, disconnects/restarts the source and verifies immediate `unknown`/lower-bound. A later
matching closed snapshot restores the independently expected exact count without pausing terminal input.

### E2E-03 — portable cross-provider handoff

A large source conversation is handed to a different provider. The operator or pre-authorised Flow reviews
one target-budgeted packet containing older digest, recent exact tail and a gated full artifact reference.
The target receives it once, recapitulates without automatic execution and shows new instance plus handoff
lineage. A disconnect after possible submission becomes uncertain and is not replayed.

### E2E-04 — one attention route under active input

While the operator types in a TUI and then records a voice draft, children across providers emit a permission,
question, failure and completed result. Automatic focus is deferred without loss; ordering remains stable.
Manual Next Attention cancels capture without retargeting its draft and lands on the exact action owner.
Navigation changes no demand. Submission resolves only after the corresponding adapter evidence.

### E2E-05 — durable local and remote recovery

The UI restarts, daemon restarts, local host reboots and a remote target disconnects at each external-effect
boundary. Warm reattach preserves the same runtime; cold reconstruction is labelled separately; uncertain
effects do not repeat; remote work never launches locally. Semantic nodes, lineage, receipts and Attention
remain honest even where the runtime is Lost.

### E2E-06 — authoritative ending and cleanup

Ending a Session with an unresponsive local process, unreachable remote process and active companion removes
the Session from the active tree immediately, fences all clients and records survivors/pending cleanup in
history/status. Neither reconnect nor stale mutation resurrects it. No external worktree, branch or file is
deleted without a separate authorised cleanup.

### E2E-07 — high-volume operator loop

At the full scale envelope, the operator creates a Flow, filters/searches the tree, visits Attention, switches
Node Views, types in a noisy terminal and reviews results. Background transcripts, usage collectors, logs and
previews remain bounded. p50/p95/p99 input, route and view-switch latency plus memory/disk/queue evidence are
recorded, and resource pressure never terminates work.

### E2E-08 — setup to isolated work without trapping `main`

On a clean machine the operator skips one provider, authenticates another outside Turn, adopts a pinned
remote target and launches direct, Template and Flow work. Cancelled/failed probes leave no credentials or
resources. Every lifecycle path uses isolated worktrees, the primary checkout can switch branches throughout
and a seeded live v4 MainCheckout must be resolved before the migration/release can report success.

### E2E-09 — multi-client, companion and portable import

Two desktop surfaces and an offline companion race edge edits, Attention actions, input lease handoff and
Session deletion across journal compaction. They converge under the declared conflict rules without byte
interleaving or resurrection; stale companion actions refuse. Importing a hostile package with colliding ids
remints inert objects, grants no authority and creates no process.

### E2E-10 — shared runtime, accounts and lost-work recovery

The mandatory fixture launches five independent instances through one multiplexed provider endpoint whose
input has no account field and binds them to `ProviderAccountScope=endpoint_unscoped`; it creates no
AccountProfile or quota attribution. Each keeps its conversation, transcript cursor, input, context and
Attention subject. When a provider explicitly supports cross-profile multiplexing, an additional fixture
mixes three bindings for AccountProfile A and two for B on that same endpoint and proves quota/profile
isolation; that optional proof is never required of an endpoint-unscoped provider. The endpoint restarts
while an unrelated target-wide inventory discovers a known handle, an unmatched survivor and a same-named
handle on another host. Exact reconciliation restores valid bindings, refuses a duplicate conversation
claim, adopts only the selected survivor and terminates only the exact target+handle; changing an optional
default account affects one later profiled launch and no active instance.

### E2E-11 — living brief, delegated artifacts and remote operation

A reviewed Flow lets a conductor update one bounded Note brief and publish typed progress/resources. Linked
agents pull only allowed Note revisions within cumulative budgets; a hostile update, redirect and content-as-
control attempt fail. Desktop and a full remote surface see the same tree, board metadata, Node Views,
status and Attention revisions. A revisioned remote file edit conflicts safely with an external change;
writer handoff converges, one exact foreground-granted typed permission response succeeds, and every
credential/grant/admin/trust/publish action remains unavailable remotely.

### E2E-12 — external work, conversation history, native jobs and browser

From one Workspace the operator pages a rate-limited external issue source, resolves one field conflict and
reopens a closed card; selects a safe empty Session once and receives its ready default Shell without another
action; searches two isolated AccountProfile conversation inventories, adopts one historical conversation
without launch and resumes it separately; and monitors a provider-native scheduled job across daemon restart.
The same tree routes every selected card, adopted conversation, Job Node and Browser Node to one WorkSurface;
job iterations remain selected records inside the one JobNodeView or its exact provisional Attention view.
An inert WebPreview executes nothing; reviewed local HTML/localhost works in the isolated Browser while a
rebinding, popup/download bypass and ambient-credential attempt fail. No card/job dismissal changes external
source state, and no partial inventory claims zero.

### E2E-13 — delegated remote permission and profile activity truth

Desktop issues a single-use grant for one exact provider permission. A remote surface answers it over the
encrypted typed path while stale/replayed/widened options and raw PTY bytes are refused; credential, host
trust, administration, grant and publish operations remain local-only. At the same time desktop and companion
show two profiles' title/read-versus-rename capabilities, context usage, quota windows and activity inboxes.
Provider timeout and a partial cursor mark only the affected facts stale/unknown and never show false zero,
cross-profile data or an independent Attention queue.

### E2E-14 — recursive agent-per-branch tree and Workspace onboarding

Starting from no Workspace, one resumable operation clones a repository into an exact target, creates a
Session-owned CheckoutScope and projects it through nested Groups; a second path adopts an SSH repository.
The operator creates, reorders and promotes a depth-128 subtree while a concurrent cycle attempt fails
atomically. New descendants inherit the scope default, but moving an existing live child changes no cwd.
After external worktree deletion, failed inventory remains unknown and a complete inventory yields `missing`;
reconcile/unbind retains the adopted worktree and every runtime, while an explicitly reviewed remove refuses
dirty/unpublished/dangerous paths. Crash/cancel at each onboarding phase repeats no clone and performs no
publish. The primary `main` checkout stays available and a corrupt persisted cycle cannot hang restoration.

### E2E-15 — background Attention, resource pressure and routed model truth

All six dedicated adapters exercise the independent `model_gateway` capability cell. At least two adapter/
profile combinations with current supported evidence start through independently scoped ModelEndpointProfiles;
every unsupported/degraded/unknown combination refuses without fallback while Kimi/MiniMax quota-only
connectors update two profiles and expose no launch path. One missing secret and one late discovery fail
without fallback; the valid LaunchReceipts retain exact effective route/model/credential generation. Local and remote resource
snapshots then attribute current, closed-Session and unmatched processes, preserve partial/error versus zero
and permit only one exact re-probed termination. Two same-titled children raise demands while the GUI is
backgrounded: revocable endpoint grants produce two minimal encrypted notifications, duplicate replay
collapses only its exact revision, delivery failure resolves nothing, and live end fences a late tick. A deep
link resynchronises into the one WorkSurface. A generated Group/Node name is applied only at its current
revision; a pinned operator alias survives provider rename/reconnect and no terminal bytes are sent.

## Completion report

The final implementation report must list, for the exact merged commit:

1. the frozen 112-capability ledger snapshot/tree digest and disposition totals, the selector-v1 source census, the 185-requirement count and
   zero unmatched requirement/acceptance ids;
2. status and evidence artifact for every `PRD-*` row;
3. provider capability matrix with fixture and live-evidence links;
4. results for every named end-to-end journey and the recovery/security/accessibility matrices;
5. independent adversarial audit findings and dispositions;
6. CI checks and `main` commit proof.

Any unmet row is reported as a gap. It is never converted into a completion claim by changing wording,
reducing the test surface or calling an accepted design “implemented”.
